# JfireBoot multipart/form-data 解析健壮性修复需求

## 1. 需求概述

1. 联合修复 JfireBoot 与 Jnet 在 `multipart/form-data` 请求解析链路上的健壮性问题，避免在路由匹配和业务方法调用之前出现底层 buffer 越界、空指针、字符串越界或请求无响应。
2. 保持现有合法 multipart 请求的行为不变：标准文本字段应写入 `HttpRequestExtend.paramMap`，标准文件字段应写入 `HttpRequestExtend.fileParts`，业务接口可继续通过方法入参获取表单字段和 `List<FilePart>`。
3. 对请求协议或 multipart body 格式错误返回明确的客户端错误响应，而不是返回 `200 OK` 和 `error:...` 文本。
4. 修复 multipart 解析异常路径中的资源释放问题，确保请求体和 part slice 在成功、失败两类路径下都有明确所有权和释放规则。
5. 增加框架级测试覆盖问题思考文档中的最小复现和相邻复现路径，保证畸形请求不会卡住、不会抛出非预期运行时异常、不会泄露 buffer。

## 2. 功能详情

6. `HttpRequestExtend.from()` 在请求头 `Content-Type` 的 media type 为 `multipart/form-data` 时，必须进入 multipart 解析；media type 匹配应大小写不敏感，并允许 `Content-Type` 包含参数。
7. multipart 解析必须正确解析 `Content-Type` 参数，至少支持 `boundary=abc`、`boundary="abc"`、`boundary=abc; charset=UTF-8` 三类形式。
8. multipart 解析必须拒绝缺失、空值、格式非法或超出长度限制的 boundary，并产生受控的 Bad Request 类错误。
9. multipart 边界推进逻辑必须显式区分起始 boundary、普通 part 分隔 boundary 和结束 boundary，不得依赖未校验的固定下标推进读指针。
10. multipart 解析在读取任意字节、跳过 CRLF、查找下一段 boundary、创建 slice 之前，必须先确认剩余数据长度足够。
11. part header 解析必须能识别 header 结束位置；遇到 header 片段过短、孤立 `\r`、缺少完整 CRLF、header 行缺少冒号等格式错误时，必须停止解析并产生受控错误，不得让底层 `IoBuffer` 越界读取。
12. part header 解析必须防止循环不推进；每次解析 header 行后读指针必须前进，无法前进时必须失败退出。
13. part header 解析应设置防护上限，至少包括单行 header 长度、part header 总长度、header 数量三个维度，超过限制时产生受控错误。
14. `Content-Disposition` 解析必须校验 header 存在、类型为 `form-data`、包含非空 `name` 参数；缺失或非法时必须产生受控 Bad Request 类错误。
15. `Content-Disposition` 参数解析必须支持常规分号参数格式，并处理参数名大小写不敏感、参数值 trim、quoted-string 去引号等规则。
16. 文件字段识别必须继续支持 `filename` 和 `filename*`；当文件名参数格式不合法，例如缺少结束引号或编码格式非法时，必须产生受控 Bad Request 类错误。
17. 文本字段解析完成后必须释放对应 slice；文件字段交给 `FilePart` 后由 `FilePart.close()` 释放；解析失败时必须释放当前 slice 和请求 body。
18. 解析到结束 boundary 后应正常结束 multipart 解析；只有结束 boundary 且没有 part 的请求可以继续保持被容忍，除非后续技术方案明确选择更严格的协议行为。
19. Jnet 仓库必须修复 `cc.jfire.jnet.common.util.HttpCoderUtil.findAllHeaders()` 的边界检查和循环推进问题，使其在 header 不完整、缺少冒号、缺少 CRLF、只有孤立 `\r` 等情况下可失败、可返回，不再触发底层 `IoBuffer` 越界读取或解析循环不推进。

## 3. 业务规则

20. 合法 multipart 请求必须正常进入路由匹配和业务方法调用，不能因为本次修复改变参数绑定结果。
21. 畸形 multipart 请求应在框架请求解析层被拒绝，不应进入业务 Controller，也不应继续传递给 404 或 405 逻辑。
22. 请求路径是否存在不影响 multipart 格式校验；只要请求体格式非法，都应在路由匹配前得到一致的 Bad Request 类结果。
23. `boundary` 参数解析遵循 HTTP header 参数的常规行为：media type 和参数名大小写不敏感，参数值去除首尾空白，quoted-string 形式解包后参与匹配。
24. 对不支持或非法的换行格式，默认按错误请求处理；本需求不要求兼容 LF-only multipart body。
25. 解析错误信息应面向客户端和排查人员可理解，描述具体错误类型，例如缺失 boundary、boundary 不匹配、part header 非法、缺失 `Content-Disposition`、缺失 `name` 参数等。

## 4. 输入输出

26. 输入为 Jnet 聚合后的 `HttpRequest`，重点包含 method、path、headers、contentLength、contentType 和 body。
27. 合法文本 part 的输出为 `paramMap` 中的键值对，键来自 `Content-Disposition` 的 `name` 参数，值为该 part body 的 UTF-8 字符串。
28. 合法文件 part 的输出为 `fileParts` 中的 `FilePart` 对象，至少保留 `fieldName`、`fileName` 和 `ioBuffer`。
29. multipart 解析失败时，输出为 HTTP 错误响应；请求格式错误返回 `400 Bad Request`，请求体或 header 超出限制返回 `413 Payload Too Large`，框架未预期异常返回 `500 Internal Server Error`。
30. 错误日志必须包含 method、url/path、Content-Type、Content-Length、User-Agent、远端地址中当前框架能够取得的字段；当 `HttpRequestExtend.getPath()` 尚未生成时，也必须能记录原始请求路径。

## 5. 异常处理

31. multipart 协议错误必须转换为受控异常或等价错误结果，不得向外暴露 `NullPointerException`、`StringIndexOutOfBoundsException`、底层 `IoBuffer` 越界异常或无语义的 `IllegalArgumentException`。
32. `PathRequestForwardProcessor` 捕获到客户端请求格式错误时，必须设置正确 HTTP 状态码和 reason phrase，不得默认返回 `200 OK`。
33. `PathRequestForwardProcessor` 捕获到非预期框架异常时，应返回 `500 Internal Server Error`，并继续记录完整异常栈。
34. multipart 解析过程中任一步失败，都必须通过 `try/finally` 或等价机制释放已取得所有权的 `body` 和临时 slice。
35. 请求关闭时，`HttpRequestExtend.close()` 必须仍能释放所有文件 part；重复关闭或失败后关闭不得再次抛出资源释放异常。

## 6. 技术要求

36. 本需求涉及两个代码仓库：JfireBoot 仓库 `/Users/linbin/Documents/代码/JfireBoot` 与 Jnet 仓库 `/Users/linbin/Documents/代码/Jnet`，后续技术方案和任务拆分必须分别列出两个仓库的改动范围。
37. Jnet 侧修改必须集中在 `cc.jfire.jnet.common.util.HttpCoderUtil.findAllHeaders()` 及其必要测试上，目标是让通用 HTTP header 解析器具备剩余长度检查、冒号检查、CRLF 检查、读指针推进检查和受控失败能力。
38. Jnet 侧修改必须兼容 `HttpRequestPartDecoder` 与 `HttpResponsePartDecoder` 的正常请求/响应头解析；合法 HTTP header 的解析结果和 header 名标准化行为不能回退。
39. Jnet 侧不应通过放宽 `UnPooledBuffer` 越界读取语义来掩盖问题；底层 buffer 越界保护应保留，调用方必须在读取前完成长度判断。
40. Jnet 侧应增加针对 `HttpCoderUtil.findAllHeaders()` 的单元测试，覆盖合法 header、空 header 块、孤立 `\r`、缺少 CRLF、header 行无冒号、header value 缺失、读指针不推进风险等场景。
41. JfireBoot 侧修改必须集中在 `cc.jfire.boot.http.HttpRequestExtend`、`cc.jfire.boot.http.FilePart`、`cc.jfire.boot.forward.path.PathRequestForwardProcessor` 及必要的错误类型/测试代码中，避免影响无关路由、JSON、urlencoded、WebSocket 和 SSE 行为。
42. JfireBoot 侧必须基于修复后的 Jnet header 解析能力，或在 multipart part header 场景增加更严格的专用校验逻辑；无论采用哪种方式，part header 畸形输入都必须被转换为 JfireBoot 可识别的客户端请求错误。
43. JfireBoot 侧新增的 Content-Type 参数解析和 Content-Disposition 参数解析应使用结构化解析逻辑，不得继续依赖固定下标和简单 `substring` 截取完整 header 尾部。
44. JfireBoot 侧需要为 multipart parser 定义明确的最大值常量或配置入口；若暂不开放配置，也必须在代码中集中定义默认限制，便于后续调整。
45. JfireBoot 侧应增加 multipart 解析单元测试或最小 HTTP 流程测试；如果需要启动本地服务，应保证测试可自动启动和关闭服务，不依赖人工 curl。
46. 两个仓库的测试都应纳入验收：先在 Jnet 仓库验证底层 header 解析，再在 JfireBoot 仓库验证 multipart 请求解析和 HTTP 错误响应。
47. 两个仓库的修复都不得引入新的第三方依赖，除非后续技术方案证明标准库和现有 Jfire/Jnet API 无法满足需求。

## 7. 验收标准

48. Jnet 中 `HttpCoderUtil.findAllHeaders()` 对合法 HTTP header 块保持原有解析能力，`HttpRequestPartDecoder` 和 `HttpResponsePartDecoder` 的既有测试全部通过。
49. Jnet 中 `HttpCoderUtil.findAllHeaders()` 遇到空 header 块、孤立 `\r`、缺少完整 CRLF、header 行没有冒号时，不会触发 `UnPooledBuffer` 越界异常，也不会出现读指针不推进导致的卡住。
50. Jnet 中新增 header 解析测试覆盖合法 header、空 header 块、孤立 `\r`、缺少 CRLF、header 行无冒号、header value 缺失等场景。
51. 标准 multipart 文本字段请求可以正常解析，业务层能拿到字段值。
52. 标准 multipart 文件字段请求可以正常解析，业务层能拿到 `FilePart`，请求结束后文件 buffer 可被释放。
53. `boundary="abc"` 且 body 使用 `--abc` 的请求可以正常解析。
54. `boundary=abc; charset=UTF-8` 且 body 使用 `--abc` 的请求可以正常解析。
55. 缺失 boundary、空 boundary、起始 boundary 不匹配的请求返回 `400 Bad Request`，不会进入业务层。
56. part header 为空、只有孤立 `\r`、缺少完整 CRLF、header 行没有冒号的请求返回 `400 Bad Request`，不会卡住。
57. LF-only multipart body 返回 `400 Bad Request`，不会卡住。
58. 缺少 `Content-Disposition`、`Content-Disposition` 不是 `form-data`、缺少 `name`、`name` 为空的请求返回 `400 Bad Request`，不会抛出 NPE 或字符串越界。
59. `filename` 或 `filename*` 格式非法的请求返回 `400 Bad Request`，不会抛出字符串越界。
60. 只有结束 boundary 且没有 part 的请求保持可被解析完成，并继续进入后续路由流程。
61. 问题思考文档中的最小复现请求不再返回 `200 OK`，而是返回 `400 Bad Request`。
62. 对不存在路径发送畸形 multipart 请求时，返回 multipart 格式错误对应的 `400 Bad Request`；对不存在路径发送合法 multipart 请求时，仍按现有 404 流程处理。
63. 畸形 multipart 请求在测试超时时间内必须完成响应，不出现客户端等待到超时的情况。
64. JfireBoot 单元测试或集成测试覆盖问题思考文档复现矩阵中的全部用例，并断言合法请求、非法请求状态码、无卡住、无非预期异常四类结果。
65. 分别执行 Jnet 与 JfireBoot 项目测试命令后，两个仓库的新增测试和既有测试全部通过。
