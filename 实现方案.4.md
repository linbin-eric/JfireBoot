# 实现方案 4：重写 JfireBoot multipart boundary 状态机

## 对应需求
- 需求编号：1, 2, 4, 5, 9, 10, 17, 18, 20, 21, 22, 24, 27, 28, 34, 35, 44, 45, 51, 52, 55, 56, 57, 60, 61, 62, 63, 64
- 需求描述：用显式 multipart boundary 状态机替换当前基于 `indexOf(boundary)` 和固定下标推进的实现，保证合法请求兼容、畸形请求受控 400、异常路径释放资源。

## 对应技术步骤
- 技术步骤：步骤 4 - 重写 JfireBoot multipart boundary 状态机
- 拆分原因：单一实现单元

## 技术方案依据
技术方案要求 `parseMultiPart` 显式区分起始 boundary、普通 part 分隔和结束 boundary。`body` 由 `parseMultiPart` 拥有并在 finally 中释放；part slice 由文本解析释放，文件 part 交给 `FilePart.close()` 释放。LF-only、缺 CRLF、找不到合法结束分隔都必须返回 400。

## 涉及文件
| 文件路径 | 修改类型 | 修改说明 |
|---------|---------|---------|
| `src/main/java/cc/jfire/boot/http/HttpRequestExtend.java` | 修改 | 替换 `parseMultiPart`，新增 boundary 状态机辅助方法 |
| `src/main/java/cc/jfire/boot/http/FilePart.java` | 修改/确认 | 保持 `close()` 释放文件 slice |
| `src/test/java/MultipartExpectedBehaviorTest.java` | 修改/确认 | boundary 状态机相关用例应通过 |

## 代码改动指导
- **新增内容**：`parseMultiPart(IoBuffer body, String boundary)`、boundary 匹配辅助方法、ASCII 转换方法。
- **修改内容**：删除旧 `parseMultiPart(IoBuffer body)` 方法，后续 `parseBoundaryPart` 需要改为返回 slice 是否转交给文件 part。
- **删除内容**：删除旧逻辑中的 `body.addReadPosi(boundary.length + 2)` 固定推进和 `body.slice(boundaryIndex - body.getReadPosi() - 2)`。
- **代码完整性要求**：必须给出完整 `parseMultiPart` 方法和所有辅助方法。
- **完整代码或完整改动单元**：

~~~java
// 文件：src/main/java/cc/jfire/boot/http/HttpRequestExtend.java
// 替换原 parseMultiPart(IoBuffer body)。
private void parseMultiPart(IoBuffer body, String boundary)
{
    byte[] delimiter = ascii("--" + boundary);
    byte[] nextDelimiterPrefix = ascii("\r\n--" + boundary);
    IoBuffer currentSlice = null;
    fileParts = new ArrayList<>();
    getNotNullParamMap();
    try
    {
        if (!startsWith(body, delimiter))
        {
            throw HttpRequestParseException.badRequest("Invalid multipart data: boundary not found at expected position");
        }
        body.addReadPosi(delimiter.length);
        if (consumeFinalBoundarySuffix(body))
        {
            consumeOptionalCrlf(body);
            return;
        }
        consumeRequiredCrlf(body, "Invalid multipart data: missing CRLF after boundary");

        while (true)
        {
            int partStart = body.getReadPosi();
            int boundaryStart = findNextBoundary(body, nextDelimiterPrefix);
            if (boundaryStart == -1)
            {
                throw HttpRequestParseException.badRequest("Invalid multipart data: next boundary not found");
            }
            currentSlice = body.slice(boundaryStart - partStart);
            IoBuffer partSlice = currentSlice;
            currentSlice = null;
            parseBoundaryPart(partSlice);

            body.addReadPosi(nextDelimiterPrefix.length);
            if (consumeFinalBoundarySuffix(body))
            {
                consumeOptionalCrlf(body);
                return;
            }
            consumeRequiredCrlf(body, "Invalid multipart data: missing CRLF after part boundary");
        }
    }
    catch (Throwable e)
    {
        if (currentSlice != null)
        {
            currentSlice.free();
        }
        close();
        throw e;
    }
    finally
    {
        body.free();
    }
}

private static byte[] ascii(String value)
{
    return value.getBytes(StandardCharsets.US_ASCII);
}

private static boolean startsWith(IoBuffer buffer, byte[] bytes)
{
    if (buffer.remainRead() < bytes.length)
    {
        return false;
    }
    int readPosi = buffer.getReadPosi();
    for (int i = 0; i < bytes.length; i++)
    {
        if (buffer.get(readPosi + i) != bytes[i])
        {
            return false;
        }
    }
    return true;
}

private static boolean consumeFinalBoundarySuffix(IoBuffer buffer)
{
    if (buffer.remainRead() >= 2 && buffer.get(buffer.getReadPosi()) == '-' && buffer.get(buffer.getReadPosi() + 1) == '-')
    {
        buffer.addReadPosi(2);
        return true;
    }
    return false;
}

private static void consumeRequiredCrlf(IoBuffer buffer, String message)
{
    if (buffer.remainRead() < 2 || buffer.get(buffer.getReadPosi()) != '\r' || buffer.get(buffer.getReadPosi() + 1) != '\n')
    {
        throw HttpRequestParseException.badRequest(message);
    }
    buffer.addReadPosi(2);
}

private static void consumeOptionalCrlf(IoBuffer buffer)
{
    if (buffer.remainRead() >= 2 && buffer.get(buffer.getReadPosi()) == '\r' && buffer.get(buffer.getReadPosi() + 1) == '\n')
    {
        buffer.addReadPosi(2);
    }
}

private static int findNextBoundary(IoBuffer body, byte[] nextDelimiterPrefix)
{
    int scanStart = body.getReadPosi();
    int writePosi = body.getWritePosi();
    for (int i = scanStart; i + nextDelimiterPrefix.length <= writePosi; i++)
    {
        if (!matchesAt(body, i, nextDelimiterPrefix))
        {
            continue;
        }
        int after = i + nextDelimiterPrefix.length;
        if (after + 1 >= writePosi)
        {
            throw HttpRequestParseException.badRequest("Invalid multipart data: incomplete boundary");
        }
        byte b0 = body.get(after);
        byte b1 = body.get(after + 1);
        if ((b0 == '\r' && b1 == '\n') || (b0 == '-' && b1 == '-'))
        {
            return i;
        }
    }
    return -1;
}

private static boolean matchesAt(IoBuffer buffer, int pos, byte[] bytes)
{
    for (int i = 0; i < bytes.length; i++)
    {
        if (buffer.get(pos + i) != bytes[i])
        {
            return false;
        }
    }
    return true;
}
~~~

## 详细实现步骤

> 步骤状态使用 `[ ]` / `[x]` 跟踪。`tasks` 生成时全部为 `[ ]`；`implement` 每完成一个步骤后立即更新本清单。

### 实现步骤清单

- [x] 步骤 1：替换 multipart 主解析方法
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：旧 `parseMultiPart(IoBuffer body)` 方法
  - **修改内容**：删除旧方法，新增 `parseMultiPart(IoBuffer body, String boundary)` 完整方法。
  - **完成判定**：方法签名与实现方案 3 中 `from()` 的调用一致。
  - **说明**：本方法负责 `body.free()`。
- [x] 步骤 2：新增 boundary 辅助方法
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：`parseMultiPart` 方法之后
  - **修改内容**：新增 `ascii`、`startsWith`、`consumeFinalBoundarySuffix`、`consumeRequiredCrlf`、`consumeOptionalCrlf`、`findNextBoundary`、`matchesAt`。
  - **完成判定**：所有读取 `readPosi + 1` 前都确认 `remainRead() >= 2`。
  - **说明**：这些方法必须只处理 CRLF，不兼容 LF-only。
- [x] 步骤 3：调整 `parseBoundaryPart` 的 slice 所有权契约
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：`parseBoundaryPart` 方法签名
  - **修改内容**：明确 `parseBoundaryPart` 接收 slice 后拥有该 slice 所有权；文本字段和异常路径由 `parseBoundaryPart` 释放，文件字段由 `FilePart.close()` 释放。
  - **完成判定**：`parseMultiPart` 在调用 `parseBoundaryPart(partSlice)` 前将 `currentSlice` 置为 null，避免异常路径重复释放。
  - **说明**：实现方案 5 可以让 `parseBoundaryPart` 返回 `boolean` 或 `void`，但所有权规则必须保持一致。
- [x] 步骤 4：异常路径释放验证
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：`parseMultiPart` catch/finally
  - **修改内容**：保证 catch 中释放当前未转交 slice，并调用 `close()` 释放已加入的文件 part；finally 释放 body。
  - **完成判定**：任意 `HttpRequestParseException` 路径不再依赖正常循环结束释放 body。
  - **说明**：避免文件 part 已加入列表后异常导致引用泄露。
- [x] 步骤 5：运行 boundary 相关测试
  - **修改文件**：无
  - **修改位置**：无
  - **修改内容**：执行 `mvn -q -Dtest=MultipartExpectedBehaviorTest#standardTextPartShouldBeParsed,standardFilePartShouldBeParsed,finalBoundaryOnlyShouldBeTolerated,lfOnlyPartShouldReturnBadRequestWithoutHanging,initialBoundaryMismatchShouldReturnBadRequest,missingCrlfAfterBoundaryShouldReturnBadRequest test`。
  - **完成判定**：上述用例通过，或只剩实现方案 5 负责的 part header / Content-Disposition 失败。
  - **说明**：全量通过依赖实现方案 5。

## 依赖关系
- 前置实现方案：实现方案.2.md，实现方案.3.md
- 后续实现方案：实现方案.5.md，实现方案.6.md

## 测试要点
- final boundary only 被容忍。
- LF-only multipart 不再卡住。
- 起始 boundary 不匹配、boundary 后缺 CRLF 返回 400。
- 文本字段和文件字段合法基线不变。

## 验证记录
> `tasks` 生成时先保留待填写状态；`implement` / `sub-implement` 在实际执行后回填。若单个实现方案不能独立完成全局编译，必须记录原因和依赖的后续方案，不能把未验证写成已通过。

- 局部验证：通过。已执行 `mvn -q -Dtest=MultipartExpectedBehaviorTest#standardTextPartShouldBeParsed,standardFilePartShouldBeParsed,finalBoundaryOnlyShouldBeTolerated,lfOnlyPartShouldReturnBadRequestWithoutHanging,initialBoundaryMismatchShouldReturnBadRequest,missingCrlfAfterBoundaryShouldReturnBadRequest test`。
- 全局编译：通过。实现方案 6 已完成完整 multipart 验收与 JfireBoot 默认测试；实现方案 7 已完成 Jnet 与 JfireBoot 的 `mvn -q compile`。
- 未全局编译通过原因：无
- 依赖后续方案：实现方案.5.md
- 最终要求：全部实现方案完成后必须执行 `mvn compile`；若不是 Maven 项目，执行等价的只编译命令

## 注意事项
- `findNextBoundary` 只接受 `\r\n--boundary` 分隔，body 起始 boundary 由入口单独处理。
- 如果 part body 内容中出现类似 boundary 的普通文本，只有后续字节是 `\r\n` 或 `--` 时才当作合法 boundary。
- `body.slice(length)` 会推进 body 读指针；后续推进需要基于 slice 后的位置。
