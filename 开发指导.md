# 目标

1. 为 Path 增加一个 method 属性,该属性的值是 http 的方法,采用枚举类表示.
2. 在路由的过程中,需要能够核对 Path 中的 method 和 http 请求的 method,来判断是否允许路由.

# 核心思路

1. **创建 HTTP 方法枚举类**：定义 `HttpMethod` 枚举，包含常用的 HTTP 方法（GET、POST、PUT、DELETE、PATCH、HEAD、OPTIONS），以及一个 `ALL` 值表示匹配所有方法。

2. **扩展 Path 注解**：在 `@Path` 注解中新增 `method` 属性，类型为 `HttpMethod`，默认值为 `HttpMethod.ALL`（保持向后兼容）。

3. **PathRequest 存储方法信息**：在 `PathRequest` 类中新增字段存储注解中配置的 HTTP 方法。

4. **路由匹配时校验方法**：在 `PathRequestForwardProcessor` 中，当路由匹配成功后，增加 HTTP 方法的校验逻辑。如果方法不匹配，则继续查找下一个匹配的路由或返回 405 Method Not Allowed。

# 详细思路

## 1. HttpMethod 枚举设计

创建枚举类 `cc.jfire.boot.forward.path.HttpMethod`，包含以下值：
- `GET` - 对应 HTTP GET 请求
- `POST` - 对应 HTTP POST 请求
- `PUT` - 对应 HTTP PUT 请求
- `DELETE` - 对应 HTTP DELETE 请求
- `PATCH` - 对应 HTTP PATCH 请求
- `HEAD` - 对应 HTTP HEAD 请求
- `OPTIONS` - 对应 HTTP OPTIONS 请求
- `ALL` - 特殊值，匹配所有 HTTP 方法（默认值，保持向后兼容）

枚举需要提供一个方法 `matches(String requestMethod)` 用于判断是否匹配请求的 HTTP 方法。

## 2. Path 注解扩展

修改 `@Path` 注解，新增 `method` 属性：
- 属性类型：`HttpMethod`
- 默认值：`HttpMethod.ALL`
- 这样现有代码无需修改即可继续工作

## 3. PathRequest 类修改

在 `PathRequest` 类中：
- 新增 `HttpMethod httpMethod` 字段
- 在构造函数中从 `@Path` 注解读取 `method` 属性值
- 新增 `matchesMethod(String requestMethod)` 方法用于判断是否匹配

## 4. PathRequestForwardProcessor 路由逻辑修改

修改 `handleHttpRequest` 方法中的路由匹配逻辑：

**精确路径匹配部分**：
- 当前是通过 `requestMap.get(path)` 直接获取 `PathRequest`
- 需要修改为：即使路径匹配，也要校验 HTTP 方法是否匹配
- 如果方法不匹配，需要继续查找其他可能匹配的路由

**RESTful 路由匹配部分**：
- 在 `restfulMatch.match()` 成功后，增加 HTTP 方法校验
- 如果方法不匹配，继续遍历下一个 RESTful 路由

**数据结构调整**：
- 由于同一路径可能对应多个不同 HTTP 方法的处理器，`requestMap` 的 key 需要调整
- 方案：将 key 从纯路径改为"路径+方法"的组合，或者将 value 改为 `List<PathRequest>`
- 推荐方案：将 value 改为 `List<PathRequest>`，在匹配时遍历列表找到方法匹配的处理器

## 5. 方法不匹配的处理

当路径匹配但方法不匹配时：
- 如果存在该路径的其他方法处理器，返回 405 Method Not Allowed
- 如果该路径不存在任何处理器，继续原有的 404 Not Found 逻辑
