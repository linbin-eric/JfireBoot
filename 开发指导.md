# 目标

FormObjectParse 和 JsonParse 合并为一个类，命名为 `UnifiedObjectParse`。移除 `RequestType` 枚举、`@UrlFormPost` 和 `@MultiPartPost` 注解的依赖，改为运行时根据 `HttpRequestExtend.contentType` 动态判断请求类型并执行相应的解析逻辑。

# 核心思路

1. 通过 `HttpRequestExtend.contentType` 在运行时判断本次请求的类型，来决定实际上采用 Json 解析方法请求体，还是通过 paramMap 的方式来解析。
2. 属性 `PathRequest.needDeserializateJsonToParamMap` 应该在解析请求体转化为对象的时候被读取使用。
3. 移除编译时的注解判断，所有请求类型的判断都在运行时通过 `contentType` 完成。

# 详细思路

## 1. 现有代码结构分析

### 1.1 相关类和位置
- `PathRequest` 类位于 `cc.jfire.boot.forward.path.PathRequest`
- `JsonParse` 是 `PathRequest` 的内部类（第186-208行），用于处理 JSON 格式请求体
- `FormObjectParse` 是 `PathRequest` 的内部类（第237-310行），用于处理 multipart/form-data 格式请求
- `HttpRequestExtend` 类位于 `cc.jfire.boot.http.HttpRequestExtend`

### 1.2 需要移除的内容
- `RequestType` 枚举（JsonPost、UrlFormPost、MultiPost）
- `PathRequest.requestType` 属性
- 构造函数中基于 `@UrlFormPost`、`@MultiPartPost` 注解的判断逻辑

### 1.3 Content-Type 与解析策略对应关系
| Content-Type | 数据位置 | 解析策略 |
|-------------|---------|---------|
| `application/json` | `utf8StrBody` (JSON字符串) | 直接反序列化或解析到 paramMap |
| `application/x-www-form-urlencoded` | `utf8StrBody` (key=value&...) | 解析到 paramMap |
| `multipart/form-data` | `paramMap` (已解析) | 直接从 paramMap 获取 |

## 2. 合并方案

### 2.1 HttpRequestExtend 扩展
1. 新增 `parseUrlEncodedBodyToParamMap()` 方法，用于解析 `application/x-www-form-urlencoded` 格式
2. 新增 `ensureParamMapReady()` 方法，根据 `contentType` 自动调用对应的解析方法：
   - `application/json` 且需要解析到 paramMap → 调用 `parseJsonBodyToParamMap()`
   - `application/x-www-form-urlencoded` → 调用 `parseUrlEncodedBodyToParamMap()`
   - `multipart/form-data` → 无需额外操作（已在 `from()` 中解析）

### 2.2 创建 UnifiedObjectParse 类
新建 `UnifiedObjectParse` 内部类，统一处理所有请求类型的对象解析：

1. **构造函数**：接收目标类型 `Class` 和 `Type`，初始化属性访问器和值生成器
2. **apply 方法**：
   - 判断 `contentType` 是否为 `application/json` 且 `needDeserializateJsonToParamMap` 为 false
     - 是：直接使用 `Dson.fromString()` 反序列化整个 JSON
     - 否：从 `paramMap` 中获取值填充对象

### 2.3 PathRequest 简化
1. 移除 `RequestType` 枚举
2. 移除 `requestType` 属性
3. 修改构造函数，不再判断 `@UrlFormPost`、`@MultiPartPost` 注解
4. 修改 `invoke()` 方法，调用 `HttpRequestExtend.ensureParamMapReady()` 统一处理

## 3. 修改点汇总

1. **HttpRequestExtend 类**：
   - 新增 `parseUrlEncodedBodyToParamMap()` 方法
   - 新增 `ensureParamMapReady(boolean needDeserializateJsonToParamMap)` 方法

2. **PathRequest 类**：
   - 删除 `RequestType` 枚举
   - 删除 `requestType` 属性
   - 删除 `JsonParse` 内部类
   - 删除 `FormObjectParse` 内部类
   - 新增 `UnifiedObjectParse` 内部类
   - 修改构造函数，移除注解判断，统一使用 `UnifiedObjectParse`
   - 修改 `invoke()` 方法，调用 `ensureParamMapReady()`

3. **可选清理**（不在本次范围内）：
   - `UrlFormPost.java` 注解类
   - `MultiPartPost.java` 注解类
   - README.md 中的示例更新
