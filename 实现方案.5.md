# 实现方案 5：实现 multipart part header 与 Content-Disposition 严格解析

## 对应需求
- 需求编号：11, 12, 13, 14, 15, 16, 20, 24, 25, 27, 28, 31, 36, 40, 41, 42, 43, 47, 56, 58, 59, 64
- 需求描述：JfireBoot 需要对 multipart part header 和 `Content-Disposition` 做协议级校验，避免 NPE、字符串越界、null header、底层 buffer 越界和不返回。

## 对应技术步骤
- 技术步骤：步骤 5 - 实现 multipart part header 与 Content-Disposition 严格解析
- 拆分原因：单一实现单元

## 技术方案依据
技术方案要求 JfireBoot 在 multipart part 场景使用更严格的专用解析逻辑，先定位 `\r\n\r\n`，设置 header 总长度、单行长度、数量限制，再使用 header 参数解析器处理 `Content-Disposition`。`name=field` 与 `name="field"` 都应支持，非法 `filename`/`filename*` 返回 400。

## 涉及文件
| 文件路径 | 修改类型 | 修改说明 |
|---------|---------|---------|
| `src/main/java/cc/jfire/boot/http/HttpRequestExtend.java` | 修改 | 替换 `parseBoundaryPart`，新增 part header 与 Content-Disposition 辅助结构 |
| `src/test/java/MultipartExpectedBehaviorTest.java` | 修改/确认 | Content-Disposition 与 part header 相关用例应通过 |

## 代码改动指导
- **新增内容**：`PartDisposition`、header 限制常量、`parsePartHeaders`、`parseContentDisposition`、`decodeRange`、`findBytes`、`findCrlfBefore`。
- **修改内容**：完整替换 `parseBoundaryPart`，不再调用 `HttpCoderUtil.findAllHeaders(slice, ...)` 解析 multipart part header。
- **删除内容**：删除 `disposition.indexOf("name=")`、固定下标 substring、`filename*=UTF-8''` 直接截尾逻辑。
- **代码完整性要求**：必须给出完整 `parseBoundaryPart` 方法和所有新增辅助方法。
- **完整代码或完整改动单元**：

~~~java
// 文件：src/main/java/cc/jfire/boot/http/HttpRequestExtend.java
// 在常量区域新增。
private static final byte[] HEADER_END = "\r\n\r\n".getBytes(StandardCharsets.US_ASCII);
private static final int MAX_PART_HEADER_BYTES = 32 * 1024;
private static final int MAX_PART_HEADER_LINE_BYTES = 8 * 1024;
private static final int MAX_PART_HEADER_COUNT = 100;

private record PartDisposition(String fieldName, String fileName)
{
}

// 完整替换 parseBoundaryPart。返回 true 表示 slice 由 FilePart 接管。
private boolean parseBoundaryPart(IoBuffer slice)
{
    boolean transferred = false;
    try
    {
        Map<String, String> partHeaders = parsePartHeaders(slice);
        String partContentType = partHeaders.getOrDefault("Content-Type", "text/plain");
        PartDisposition disposition = parseContentDisposition(partHeaders.get("Content-Disposition"));
        boolean fileLike = disposition.fileName() != null || "application/octet-stream".equalsIgnoreCase(partContentType);
        if (fileLike)
        {
            FilePart filePart = new FilePart();
            filePart.setFileName(disposition.fileName());
            filePart.setFieldName(disposition.fieldName());
            filePart.setIoBuffer(slice);
            fileParts.add(filePart);
            transferred = true;
            return true;
        }
        String value = StandardCharsets.UTF_8.decode(slice.readableByteBuffer()).toString();
        paramMap.put(disposition.fieldName(), value);
        return false;
    }
    finally
    {
        if (!transferred)
        {
            slice.free();
        }
    }
}

private Map<String, String> parsePartHeaders(IoBuffer slice)
{
    int headerEnd = findBytes(slice, HEADER_END, MAX_PART_HEADER_BYTES + HEADER_END.length);
    if (headerEnd == -1)
    {
        throw HttpRequestParseException.badRequest("Invalid multipart part header");
    }
    int headerLength = headerEnd - slice.getReadPosi();
    if (headerLength > MAX_PART_HEADER_BYTES)
    {
        throw HttpRequestParseException.payloadTooLarge("Multipart part header is too large");
    }
    Map<String, String> headers = new HashMap<>();
    int pos = slice.getReadPosi();
    int count = 0;
    while (pos < headerEnd)
    {
        int lineEnd = findCrlfBefore(slice, pos, headerEnd);
        if (lineEnd == -1)
        {
            throw HttpRequestParseException.badRequest("Invalid multipart part header line");
        }
        int lineLength = lineEnd - pos;
        if (lineLength > MAX_PART_HEADER_LINE_BYTES)
        {
            throw HttpRequestParseException.payloadTooLarge("Multipart part header line is too large");
        }
        int colon = findByte(slice, pos, lineEnd, (byte) ':');
        if (colon <= pos)
        {
            throw HttpRequestParseException.badRequest("Invalid multipart part header line");
        }
        String name = decodeRange(slice, pos, colon).trim();
        if (name.isEmpty())
        {
            throw HttpRequestParseException.badRequest("Invalid multipart part header name");
        }
        int valueStart = colon + 1;
        while (valueStart < lineEnd)
        {
            byte b = slice.get(valueStart);
            if (b != ' ' && b != '\t')
            {
                break;
            }
            valueStart++;
        }
        String value = decodeRange(slice, valueStart, lineEnd).trim();
        headers.put(HttpCoderUtil.normalizeHeaderName(name), value);
        count++;
        if (count > MAX_PART_HEADER_COUNT)
        {
            throw HttpRequestParseException.payloadTooLarge("Too many multipart part headers");
        }
        pos = lineEnd + 2;
    }
    slice.setReadPosi(headerEnd + HEADER_END.length);
    return headers;
}

private PartDisposition parseContentDisposition(String raw)
{
    if (raw == null)
    {
        throw HttpRequestParseException.badRequest("Missing Content-Disposition");
    }
    HeaderValue disposition = parseHeaderValue(raw);
    if (!"form-data".equalsIgnoreCase(disposition.value()))
    {
        throw HttpRequestParseException.badRequest("Invalid Content-Disposition");
    }
    String fieldName = disposition.params().get("name");
    if (fieldName == null || fieldName.isEmpty())
    {
        throw HttpRequestParseException.badRequest("Missing multipart field name");
    }
    String fileName = null;
    String encodedFileName = disposition.params().get("filename*");
    if (encodedFileName != null)
    {
        String prefix = "UTF-8''";
        if (!encodedFileName.regionMatches(true, 0, prefix, 0, prefix.length()))
        {
            throw HttpRequestParseException.badRequest("Invalid multipart filename*");
        }
        try
        {
            fileName = URLDecoder.decode(encodedFileName.substring(prefix.length()), StandardCharsets.UTF_8);
        }
        catch (IllegalArgumentException e)
        {
            throw HttpRequestParseException.badRequest("Invalid multipart filename*");
        }
    }
    else
    {
        fileName = disposition.params().get("filename");
    }
    return new PartDisposition(fieldName, fileName);
}

private static int findBytes(IoBuffer buffer, byte[] target, int maxScanBytes)
{
    int start = buffer.getReadPosi();
    int maxEnd = Math.min(buffer.getWritePosi(), start + maxScanBytes);
    for (int i = start; i + target.length <= maxEnd; i++)
    {
        if (matchesAt(buffer, i, target))
        {
            return i;
        }
    }
    return -1;
}

private static int findCrlfBefore(IoBuffer buffer, int start, int end)
{
    for (int i = start; i + 1 < end; i++)
    {
        if (buffer.get(i) == '\r')
        {
            if (buffer.get(i + 1) == '\n')
            {
                return i;
            }
            throw HttpRequestParseException.badRequest("Invalid multipart part header line");
        }
    }
    return -1;
}

private static int findByte(IoBuffer buffer, int start, int end, byte target)
{
    for (int i = start; i < end; i++)
    {
        if (buffer.get(i) == target)
        {
            return i;
        }
    }
    return -1;
}

private static String decodeRange(IoBuffer buffer, int start, int end)
{
    int oldReadPosi = buffer.getReadPosi();
    buffer.setReadPosi(start);
    String value = StandardCharsets.UTF_8.decode(buffer.readableByteBuffer(end)).toString();
    buffer.setReadPosi(oldReadPosi);
    return value;
}
~~~

## 详细实现步骤

> 步骤状态使用 `[ ]` / `[x]` 跟踪。`tasks` 生成时全部为 `[ ]`；`implement` 每完成一个步骤后立即更新本清单。

### 实现步骤清单

- [x] 步骤 1：新增 part header 限制常量和结构
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：类常量区域
  - **修改内容**：新增 `HEADER_END`、`MAX_PART_HEADER_BYTES`、`MAX_PART_HEADER_LINE_BYTES`、`MAX_PART_HEADER_COUNT` 和 `PartDisposition`。
  - **完成判定**：常量集中定义，无魔法数散落。
  - **说明**：超限使用 413，格式错误使用 400。
- [x] 步骤 2：替换 `parseBoundaryPart`
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：旧 `parseBoundaryPart(IoBuffer slice)` 方法
  - **修改内容**：用完整改动单元中的 `private boolean parseBoundaryPart(IoBuffer slice)` 替换旧方法。
  - **完成判定**：方法不再直接读取 `disposition.indexOf("name=")`，不再抛 NPE 或字符串越界。
  - **说明**：返回 true 时 slice 由 `FilePart` 接管。
- [x] 步骤 3：新增 part header 解析方法
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：`parseBoundaryPart` 后
  - **修改内容**：新增 `parsePartHeaders`、`findBytes`、`findCrlfBefore`、`findByte`、`decodeRange`。
  - **完成判定**：空 header、无冒号、LF-only、孤立 CR 均抛 `HttpRequestParseException.badRequest(...)`。
  - **说明**：`decodeRange` 与实现方案 1 中 Jnet 方法同名但属于不同类；注意避免重复方法签名冲突。
- [x] 步骤 4：新增 Content-Disposition 解析
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：part header 辅助方法附近
  - **修改内容**：新增 `parseContentDisposition`，复用实现方案 3 的 `parseHeaderValue`。
  - **完成判定**：`name=field` 可解析；缺失 `Content-Disposition`、缺失 `name`、非法 `filename*` 返回 400。
  - **说明**：`filename*` 优先级高于 `filename`。
- [x] 步骤 5：运行 part header 相关测试
  - **修改文件**：无
  - **修改位置**：无
  - **修改内容**：执行 `mvn -q -Dtest=MultipartExpectedBehaviorTest#unquotedContentDispositionNameShouldBeParsed,emptyPartHeaderShouldReturnBadRequest,partHeaderWithoutColonShouldReturnBadRequestWithoutHanging,missingContentDispositionShouldReturnBadRequest,missingContentDispositionNameShouldReturnBadRequest,filenameMissingQuoteShouldReturnBadRequest test`。
  - **完成判定**：上述用例全部通过。
  - **说明**：如果 boundary 状态机未完成，先完成实现方案 4。

## 依赖关系
- 前置实现方案：实现方案.2.md，实现方案.3.md，实现方案.4.md
- 后续实现方案：实现方案.6.md

## 测试要点
- `name=field` 未加引号应成功。
- 缺少 `Content-Disposition`、缺少 `name` 返回 400。
- `filename` 缺少结束引号返回 400。
- part header 无冒号和 LF-only 不再卡住。

## 验证记录
> `tasks` 生成时先保留待填写状态；`implement` / `sub-implement` 在实际执行后回填。若单个实现方案不能独立完成全局编译，必须记录原因和依赖的后续方案，不能把未验证写成已通过。

- 局部验证：通过。已执行 `mvn -q -Dtest=MultipartExpectedBehaviorTest#unquotedContentDispositionNameShouldBeParsed,emptyPartHeaderShouldReturnBadRequest,partHeaderWithoutColonShouldReturnBadRequestWithoutHanging,missingContentDispositionShouldReturnBadRequest,missingContentDispositionNameShouldReturnBadRequest,filenameMissingQuoteShouldReturnBadRequest test`。
- 全局编译：通过。实现方案 6 已完成双仓库完整验证；实现方案 7 已完成 Jnet 与 JfireBoot 的 `mvn -q compile`。
- 未全局编译通过原因：无
- 依赖后续方案：无
- 最终要求：全部实现方案完成后必须执行 `mvn compile`；若不是 Maven 项目，执行等价的只编译命令

## 注意事项
- `parsePartHeaders` 设置 `slice.readPosi` 到 body 内容起点后，文件字段才能把剩余内容作为文件内容。
- 如果实现方案 4 中 `parseMultiPart` 选择让 `parseBoundaryPart` 自行释放 slice，则本方案的 finally 逻辑必须保留。
- 不要把 LF-only 兼容为合法格式，本需求明确返回 400。
