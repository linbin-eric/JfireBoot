# 详细步骤

> 本文档引用了《开发指导.md》，按照其中的设计思路，将每个代码改动点作为独立步骤详细说明。

## 步骤 1：创建 HttpMethod 枚举类

**文件位置**：`src/main/java/cc/jfire/boot/forward/path/HttpMethod.java`（新建文件）

**改动内容**：

```java
package cc.jfire.boot.forward.path;

/**
 * HTTP 请求方法枚举
 */
public enum HttpMethod
{
    GET,
    POST,
    PUT,
    DELETE,
    PATCH,
    HEAD,
    OPTIONS,
    /**
     * 匹配所有 HTTP 方法（默认值）
     */
    ALL;

    /**
     * 判断当前枚举值是否匹配指定的请求方法
     *
     * @param requestMethod 请求的 HTTP 方法字符串（如 "GET"、"POST"）
     * @return 如果匹配返回 true，否则返回 false
     */
    public boolean matches(String requestMethod)
    {
        if (this == ALL)
        {
            return true;
        }
        return this.name().equalsIgnoreCase(requestMethod);
    }
}
```

---

## 步骤 2：修改 Path 注解，新增 method 属性

**文件位置**：`src/main/java/cc/jfire/boot/forward/path/Path.java`

**当前代码**：

```java
@Retention(RetentionPolicy.RUNTIME)
@Target({ElementType.FIELD, ElementType.METHOD})
public @interface Path
{
    String value();
}
```

**修改为**：

```java
@Retention(RetentionPolicy.RUNTIME)
@Target({ElementType.FIELD, ElementType.METHOD})
public @interface Path
{
    String value();

    /**
     * 允许的 HTTP 方法，默认为 ALL（匹配所有方法）
     */
    HttpMethod[] method() default HttpMethod.ALL;
}
```

---

## 步骤 3：修改 PathRequest 类，存储并校验 HTTP 方法

**文件位置**：`src/main/java/cc/jfire/boot/forward/path/PathRequest.java`

### 3.1 新增字段

在类的字段声明区域（第 31-36 行附近）新增：

```java
private HttpMethod httpMethod;
```

### 3.2 修改构造函数

修改构造函数，接收 `HttpMethod` 参数：

```java
public PathRequest(Method method, Object host, HttpMethod httpMethod)
{
    this.method = method;
    this.host   = host;
    this.httpMethod = httpMethod;
    Path annotation = AnnotationContext.getAnnotation(Path.class, method);
    path = annotation.value();
    // ... 后续代码不变
}
```

### 3.3 新增方法匹配方法

在类中新增方法（建议放在 `invoke` 方法之前）：

```java
/**
 * 判断是否匹配指定的 HTTP 方法
 *
 * @param requestMethod 请求的 HTTP 方法字符串
 * @return 如果匹配返回 true，否则返回 false
 */
public boolean matchesMethod(String requestMethod)
{
    return httpMethod.matches(requestMethod);
}
```

### 3.4 新增获取路由 key 的方法

在类中新增方法，用于生成 `requestMap` 的 key：

```java
/**
 * 获取路由 key，格式为 "路径:HTTP方法"
 * 用于 requestMap 的 key 构建
 *
 * @return 路由 key
 */
public String getRouteKey()
{
    return path + ":" + httpMethod.name();
}
```

### 3.5 关于 httpMethod 字段的 getter

由于类已使用 `@Data` 注解，Lombok 会自动生成 getter，无需手动添加。

---

## 步骤 4：修改 PathRequestForwardProcessor 的路由匹配逻辑

**文件位置**：`src/main/java/cc/jfire/boot/forward/path/PathRequestForwardProcessor.java`

### 4.1 修改 handleHttpRequest 方法

将第 39-70 行的 `handleHttpRequest` 方法修改为：

```java
private void handleHttpRequest(HttpRequest data, ReadProcessorNode next)
{
    String path = "";
    try (HttpRequestExtend requestExtend = HttpRequestExtend.from(data, next.pipeline()))
    {
        path = requestExtend.getPath();
        String requestMethod = requestExtend.getMethod();

        // 尝试精确匹配：路径 + HTTP 方法
        String routeKey = path + ":" + requestMethod;
        PathRequest pathRequest = requestMap.get(routeKey);

        // 如果精确匹配失败，尝试匹配 ALL 方法
        if (pathRequest == null)
        {
            routeKey = path + ":ALL";
            pathRequest = requestMap.get(routeKey);
        }

        if (pathRequest != null)
        {
            Object value = pathRequest.invoke(requestExtend);
            if (value != null)
            {
                next.pipeline().fireWrite(value);
            }
            return;
        }

        // 精确路径未匹配，尝试 RESTful 路由
        Map<String, Object> paramMap = requestExtend.getNotNullParamMap();
        Map<String, Object> originalParamMap = new HashMap<>(paramMap);
        boolean restfulPathExists = false;

        for (PathRequest restfulRequest : restfulRequests)
        {
            paramMap.clear();
            paramMap.putAll(originalParamMap);
            if (restfulRequest.getRestfulMatch().match(path, paramMap))
            {
                restfulPathExists = true;
                if (restfulRequest.matchesMethod(requestMethod))
                {
                    Object value = restfulRequest.invoke(requestExtend);
                    if (value != null)
                    {
                        next.pipeline().fireWrite(value);
                    }
                    return;
                }
            }
        }

        // 检查路径是否存在（用于判断返回 404 还是 405）
        boolean pathExists = requestMap.keySet().stream().anyMatch(key -> key.startsWith(path + ":"));

        // 路径存在但方法不匹配，返回 405
        if (pathExists || restfulPathExists)
        {
            HttpResponse response = new HttpResponse();
            response.getHead().setStatusCode(405);
            response.getHead().setReasonPhrase("Method Not Allowed");
            response.setBodyText("Method Not Allowed");
            next.pipeline().fireWrite(response);
            return;
        }

        // 路径不存在，传递给下一个处理器（404）
        next.fireRead(requestExtend);
    }
    catch (Throwable e)
    {
        log.error("请求出现异常,当前请求路径:{}", path, e);
        HttpResponse response = new HttpResponse();
        response.setBodyText("error:" + e.toString());
        next.pipeline().fireWrite(response);
    }
}
```

**补充说明**：
- 上述代码新增了 `HashMap` 的使用，因此需要在 `PathRequestForwardProcessor.java` 顶部补充 import：`java.util.HashMap`。

---

## 步骤 5：修改路由注册逻辑

**文件位置**：`src/main/java/cc/jfire/boot/http/HttpAppServer.java`

### 5.1 修改 parseFromApplication 方法

将第 116-124 行的方法：

```java
private static Map<String, PathRequest> parseFromApplication(ApplicationContext context)
{
    context.makeAvailable();
    return context.getAllBeanRegisterInfos().stream()//
                  .flatMap(beanRegisterInfo -> Arrays.stream(beanRegisterInfo.getType().getDeclaredMethods()))//
                  .filter(method -> method.isAnnotationPresent(Path.class))//
                  .map(method -> new PathRequest(method, context.getBeanRegisterInfo(method.getDeclaringClass()).get().getBean()))//
                  .collect(Collectors.toMap(PathRequest::getPath, Function.identity()));
}
```

修改为：

```java
private static Map<String, PathRequest> parseFromApplication(ApplicationContext context)
{
    context.makeAvailable();
    return context.getAllBeanRegisterInfos().stream()//
                  .flatMap(beanRegisterInfo -> Arrays.stream(beanRegisterInfo.getType().getDeclaredMethods()))//
                  .filter(method -> method.isAnnotationPresent(Path.class))//
                  .flatMap(method -> {
                      Object       host        = context.getBeanRegisterInfo(method.getDeclaringClass()).get().getBean();
                      HttpMethod[] httpMethods = method.getAnnotation(Path.class).method();
                      return Arrays.stream(httpMethods).map(httpMethod -> new PathRequest(method, host, httpMethod));
                  })//
                  .collect(Collectors.toMap(PathRequest::getRouteKey, Function.identity()));
}
```

**改动说明**：
- `Path` 注解的 `method` 属性改为数组类型 `HttpMethod[]`
- 使用 `flatMap` 将每个方法根据其 `method` 数组展开为多个 `PathRequest`
- 每个 HTTP 方法对应一个独立的 `PathRequest` 实例
- 例如：`@Path(value = "/user", method = {GET, POST})` 会生成两个 `PathRequest`：`/user:GET` 和 `/user:POST`
