# 实现方案 2：建立 JfireBoot 请求解析错误模型和响应映射

## 对应需求
- 需求编号：3, 21, 22, 25, 29, 30, 31, 32, 33, 36, 41, 46, 47, 55, 56, 57, 58, 59, 61, 62, 63
- 需求描述：将 JfireBoot 请求解析层的 multipart 协议错误转换为明确的 HTTP 错误响应，避免默认返回 `200 OK` 与 Java 异常文本。

## 对应技术步骤
- 技术步骤：步骤 2 - 建立 JfireBoot 请求解析错误模型和响应映射
- 拆分原因：单一实现单元

## 技术方案依据
技术方案要求新增请求解析异常类型，multipart 协议错误统一抛出 400 或 413 类异常；`PathRequestForwardProcessor` 捕获该异常后写出正确状态码和 reason phrase。非预期异常返回 500，并保留完整日志。非法 multipart 不得继续进入 404/405。

## 涉及文件
| 文件路径 | 修改类型 | 修改说明 |
|---------|---------|---------|
| `src/main/java/cc/jfire/boot/http/HttpRequestParseException.java` | 新增 | 请求解析异常模型 |
| `src/main/java/cc/jfire/boot/forward/path/PathRequestForwardProcessor.java` | 修改 | 捕获解析异常并映射 400/413/500 |
| `src/test/java/MultipartExpectedBehaviorTest.java` | 修改/确认 | `assertBadRequest` 用例应在本方案及后续方案完成后通过 |

## 代码改动指导
- **新增内容**：新增 `HttpRequestParseException` 完整类。
- **修改内容**：修改 `PathRequestForwardProcessor` import、`handleHttpRequest` catch 分支，并新增响应构造/日志上下文辅助方法。
- **删除内容**：删除 catch-all 分支中默认不设置状态码的 `error:` 响应行为。
- **代码完整性要求**：新增文件必须完整；修改已有文件必须提供完整 `handleHttpRequest` 方法和辅助方法。
- **完整代码或完整改动单元**：

~~~java
// 新增文件：src/main/java/cc/jfire/boot/http/HttpRequestParseException.java
package cc.jfire.boot.http;

public class HttpRequestParseException extends RuntimeException
{
    private final int statusCode;
    private final String reasonPhrase;
    private final String clientMessage;

    private HttpRequestParseException(int statusCode, String reasonPhrase, String clientMessage)
    {
        super(clientMessage);
        this.statusCode = statusCode;
        this.reasonPhrase = reasonPhrase;
        this.clientMessage = clientMessage;
    }

    public static HttpRequestParseException badRequest(String message)
    {
        return new HttpRequestParseException(400, "Bad Request", message);
    }

    public static HttpRequestParseException payloadTooLarge(String message)
    {
        return new HttpRequestParseException(413, "Payload Too Large", message);
    }

    public int getStatusCode()
    {
        return statusCode;
    }

    public String getReasonPhrase()
    {
        return reasonPhrase;
    }

    public String getClientMessage()
    {
        return clientMessage;
    }
}
~~~

~~~java
// 文件：src/main/java/cc/jfire/boot/forward/path/PathRequestForwardProcessor.java
// 新增 import
import cc.jfire.boot.http.HttpRequestParseException;

// 完整替换 handleHttpRequest 方法。
private void handleHttpRequest(HttpRequest data, ReadProcessorNode next)
{
    String rawMethod = data.getHead() == null ? null : data.getHead().getMethod();
    String rawPath = data.getHead() == null ? null : data.getHead().getPath();
    String rawContentType = findHeader(data, "Content-Type");
    String rawUserAgent = findHeader(data, "User-Agent");
    long rawContentLength = data.getHead() == null ? -1 : data.getHead().getContentLength();
    String path = rawPath == null ? "" : rawPath;
    try (HttpRequestExtend requestExtend = HttpRequestExtend.from(data, next.pipeline()))
    {
        path = requestExtend.getPath();
        String requestMethod = requestExtend.getMethod();
        PathRequest[] pathRequests = specificRequestMap.get(path);
        PathRequest selected = null;
        if (pathRequests == null)
        {
            Map<String, Object> paramMap = requestExtend.getNotNullParamMap();
            Map<String, Object> originalParamMap = new HashMap<>(paramMap);
            for (PathRequest restfulRequest : restfulRequests)
            {
                paramMap.clear();
                paramMap.putAll(originalParamMap);
                if (restfulRequest.getRestfulMatch().match(path, paramMap) && restfulRequest.matchesMethod(requestMethod))
                {
                    selected = restfulRequest;
                    break;
                }
            }
            if (selected == null)
            {
                next.fireRead(requestExtend);
                return;
            }
        }
        else
        {
            for (PathRequest pathRequest : pathRequests)
            {
                if (pathRequest.matchesMethod(requestMethod))
                {
                    selected = pathRequest;
                    break;
                }
            }
            if (selected == null)
            {
                HttpResponse response = new HttpResponse();
                response.getHead().setStatusCode(405);
                response.getHead().setReasonPhrase("Method Not Allowed");
                response.setBodyText("Method Not Allowed.for request:" + path, next.pipeline().allocator());
                next.pipeline().fireWrite(response);
                return;
            }
        }
        if (selected.isWs())
        {
            IoBuffer buffer = WebSocketHandshakeUtil.buildUpgradeResponse(getSecWebSocketKey(requestExtend), next.pipeline().allocator());
            next.pipeline().fireWrite(buffer);
            selected.invoke(requestExtend);
        }
        else
        {
            Object value = selected.invoke(requestExtend);
            if (value != null)
            {
                next.pipeline().fireWrite(value);
            }
        }
    }
    catch (HttpRequestParseException e)
    {
        log.warn("请求格式错误, method:{}, path:{}, Content-Type:{}, Content-Length:{}, User-Agent:{}", rawMethod, path, rawContentType, rawContentLength, rawUserAgent, e);
        writeError(next, e.getStatusCode(), e.getReasonPhrase(), e.getClientMessage());
    }
    catch (Throwable e)
    {
        log.error("请求出现异常, method:{}, path:{}, Content-Type:{}, Content-Length:{}, User-Agent:{}", rawMethod, path, rawContentType, rawContentLength, rawUserAgent, e);
        writeError(next, 500, "Internal Server Error", "Internal Server Error");
    }
}

private void writeError(ReadProcessorNode next, int statusCode, String reasonPhrase, String message)
{
    HttpResponse response = new HttpResponse();
    response.getHead().setStatusCode(statusCode);
    response.getHead().setReasonPhrase(reasonPhrase);
    response.setBodyText(message, next.pipeline().allocator());
    next.pipeline().fireWrite(response);
}

private String findHeader(HttpRequest request, String name)
{
    if (request == null || request.getHead() == null || request.getHead().getHeaders() == null)
    {
        return null;
    }
    for (Map.Entry<String, String> entry : request.getHead().getHeaders().entrySet())
    {
        if (entry.getKey().equalsIgnoreCase(name))
        {
            return entry.getValue();
        }
    }
    return null;
}
~~~

## 详细实现步骤

> 步骤状态使用 `[ ]` / `[x]` 跟踪。`tasks` 生成时全部为 `[ ]`；`implement` 每完成一个步骤后立即更新本清单。

### 实现步骤清单

- [x] 步骤 1：新增请求解析异常类
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestParseException.java`
  - **修改位置**：新增文件
  - **修改内容**：按“完整代码或完整改动单元”新增完整类。
  - **完成判定**：类可编译，提供 `badRequest` 和 `payloadTooLarge` 两个工厂方法。
  - **说明**：后续 multipart parser 只抛该异常表示客户端请求错误。
- [x] 步骤 2：修改转发处理器异常映射
  - **修改文件**：`src/main/java/cc/jfire/boot/forward/path/PathRequestForwardProcessor.java`
  - **修改位置**：import 区域、`handleHttpRequest` 方法
  - **修改内容**：新增 `HttpRequestParseException` import，完整替换 `handleHttpRequest`。
  - **完成判定**：catch `HttpRequestParseException` 时设置响应 statusCode/reasonPhrase；catch 其他异常时设置 500。
  - **说明**：当前红灯测试中的非法 multipart 仍可能因后续 parser 未改而失败，但 200 错误响应应被消除。
- [x] 步骤 3：新增错误响应与 header 读取辅助方法
  - **修改文件**：`src/main/java/cc/jfire/boot/forward/path/PathRequestForwardProcessor.java`
  - **修改位置**：`handleHttpRequest` 方法之后、`getSecWebSocketKey` 之前
  - **修改内容**：新增 `writeError` 和 `findHeader` 方法。
  - **完成判定**：日志可读取原始 Content-Type、Content-Length、User-Agent；错误响应 body 不包含 Java 异常类名。
  - **说明**：`findHeader` 需要大小写不敏感。
- [x] 步骤 4：局部编译检查
  - **修改文件**：无
  - **修改位置**：无
  - **修改内容**：在 JfireBoot 仓库执行 `mvn -q test-compile`。
  - **完成判定**：编译通过。
  - **说明**：`MultipartExpectedBehaviorTest` 全部变绿需要等待实现方案 3-5。

## 依赖关系
- 前置实现方案：无
- 后续实现方案：实现方案.3.md，实现方案.4.md，实现方案.5.md，实现方案.6.md

## 测试要点
- 非法 multipart 不再返回 200。
- 非预期异常返回 500。
- 请求解析失败时日志 path 不再只能是空字符串。

## 验证记录
> `tasks` 生成时先保留待填写状态；`implement` / `sub-implement` 在实际执行后回填。若单个实现方案不能独立完成全局编译，必须记录原因和依赖的后续方案，不能把未验证写成已通过。

- 局部验证：通过。已在 JfireBoot 仓库执行 `mvn -q test-compile`。
- 全局编译：通过。实现方案 6 已完成 `MultipartExpectedBehaviorTest` 与 JfireBoot 默认测试；实现方案 7 已完成 Jnet 与 JfireBoot 的 `mvn -q compile`。
- 未全局编译通过原因：无
- 依赖后续方案：实现方案.3.md、实现方案.4.md、实现方案.5.md
- 最终要求：全部实现方案完成后必须执行 `mvn compile`；若不是 Maven 项目，执行等价的只编译命令

## 注意事项
- 不要吞掉 `HttpRequestExtend` 的正常 close 行为。
- `PathRequestForwardProcessor` 中现有路由选择逻辑不属于本方案，不做重构。
- WebSocket 分支保持原行为。
