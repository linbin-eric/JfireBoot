# 实现方案 7：兼容性、风险和回归边界控制

## 对应需求
- 需求编号：2, 20, 24, 33, 35, 38, 39, 41, 47, 48, 65
- 需求描述：控制 Jnet 与 JfireBoot 双仓库改动的兼容性风险，确认正常请求行为不回退，异常路径不泄露资源，非 multipart 流程不被误伤。

## 对应技术步骤
- 技术步骤：步骤 7 - 兼容性、风险和回归边界控制
- 拆分原因：单一实现单元

## 技术方案依据
技术方案要求合法 HTTP header、合法 multipart 文本字段和文件字段保持原行为；JSON、urlencoded、无 body、WebSocket、SSE 不进入新的 multipart 状态机；错误响应不泄露 Java 异常类名；所有 `IoBuffer` slice 所有权必须清晰。

## 涉及文件
| 文件路径 | 修改类型 | 修改说明 |
|---------|---------|---------|
| `/Users/linbin/Documents/代码/Jnet/src/main/java/cc/jfire/jnet/common/util/HttpCoderUtil.java` | 检查 | 确认合法 header 兼容和 buffer 越界保护不变 |
| `src/main/java/cc/jfire/boot/http/HttpRequestExtend.java` | 检查 | 确认 multipart 与非 multipart 分支隔离，slice 释放正确 |
| `src/main/java/cc/jfire/boot/forward/path/PathRequestForwardProcessor.java` | 检查 | 确认 400/413/500 映射和日志上下文 |
| `README.md` | 修改/可选 | 如行为说明需要更新，则补充错误响应和 multipart 边界格式说明 |

## 代码改动指导
- **新增内容**：无强制新增生产代码；如 README 与实际行为不一致，补充文档段落。
- **修改内容**：仅当检查发现错误响应、资源释放或文档说明不一致时做小范围修正。
- **删除内容**：不得删除现有 JSON/urlencoded/WebSocket/SSE 行为。
- **代码完整性要求**：本方案以检查和回归验证为主；如修改 README，提供完整段落；如修改代码，必须回到对应实现方案更新状态。
- **完整代码或完整改动单元**：

~~~markdown
<!-- 可选 README 补充段落，若实施时判断需要更新文档，可加入 multipart/form-data 小节附近。 -->
畸形 multipart/form-data 请求会在路由匹配前被框架拒绝并返回 400 Bad Request；
请求体或 part header 超过框架限制时返回 413 Payload Too Large。
合法请求继续按字段写入参数 Map，文件字段继续写入 List<FilePart>。
~~~

## 详细实现步骤

> 步骤状态使用 `[ ]` / `[x]` 跟踪。`tasks` 生成时全部为 `[ ]`；`implement` 每完成一个步骤后立即更新本清单。

### 实现步骤清单

- [x] 步骤 1：检查非 multipart 流程隔离
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：`from`、`ensureParamMapReady`
  - **修改内容**：确认只有 media type 精确为 `multipart/form-data` 时进入新 multipart 状态机；`application/json`、`application/x-www-form-urlencoded` 和其他 body 仍按原路径处理。
  - **完成判定**：现有 `RestfulTest`、`SSETest` 和 README 中 JSON/urlencoded 行为没有被改动。
  - **说明**：这是控制影响范围的关键。
- [x] 步骤 2：检查资源释放路径
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`、`src/main/java/cc/jfire/boot/http/FilePart.java`
  - **修改位置**：`from`、`parseMultiPart`、`parseBoundaryPart`、`close`、`FilePart.close`
  - **修改内容**：逐条检查 body、文本 slice、文件 slice、异常中当前 slice、已加入 `fileParts` 的释放责任。
  - **完成判定**：文本字段 slice 在解析后释放；文件字段 slice 只由 `FilePart.close()` 释放；异常路径调用 `close()` 释放已转交文件 part。
  - **说明**：避免双重释放或泄露。
- [x] 步骤 3：检查错误响应和日志边界
  - **修改文件**：`src/main/java/cc/jfire/boot/forward/path/PathRequestForwardProcessor.java`
  - **修改位置**：`handleHttpRequest` catch 分支
  - **修改内容**：确认 `HttpRequestParseException` 返回自身状态码；其他异常返回 500；客户端 body 不包含 Java 异常类名；日志保留完整异常栈和可取得的请求上下文。
  - **完成判定**：`MultipartExpectedBehaviorTest` 中 `assertBadRequest` 通过；手工检查 500 分支不再返回 200。
  - **说明**：不要把业务方法抛出的异常误判为 400。
- [x] 步骤 4：执行最终回归命令并记录
  - **修改文件**：`任务追踪.md`、相关 `实现方案.N.md`
  - **修改位置**：验证记录区域和任务状态
  - **修改内容**：执行实现方案 6 的所有验证命令，并回填验证记录；若 README 有更新，记录文档变更。
  - **完成判定**：所有实现方案状态与 `[x]` 勾选数一致，`任务追踪.md` 中步骤进度为 `4/4` 且阻塞点为“无”。
  - **说明**：本步骤完成后才能宣称修复闭环。

## 依赖关系
- 前置实现方案：实现方案.1.md，实现方案.2.md，实现方案.3.md，实现方案.4.md，实现方案.5.md，实现方案.6.md
- 后续实现方案：无

## 测试要点
- 非 multipart 请求不进入 multipart parser。
- `FilePart.close()` 仍是文件 slice 的唯一释放出口。
- 400/413/500 状态码语义清晰。
- 全量验证命令已执行并记录未跑项。

## 验证记录
> `tasks` 生成时先保留待填写状态；`implement` / `sub-implement` 在实际执行后回填。若单个实现方案不能独立完成全局编译，必须记录原因和依赖的后续方案，不能把未验证写成已通过。

- 局部验证：通过。已确认非 multipart 分支隔离、multipart slice 所有权、错误响应状态码和日志上下文；`MultipartExpectedBehaviorTest` 的 400 断言已覆盖非法 multipart 不进入 404/405。
- 全局编译：通过。已执行 Jnet 与 JfireBoot 的 `mvn -q compile`，并已在实现方案 6 中完成双仓库测试闭环。
- 未全局编译通过原因：无
- 依赖后续方案：无
- 最终要求：全部实现方案完成后必须执行 `mvn compile`；若不是 Maven 项目，执行等价的只编译命令

## 注意事项
- 本方案是收尾检查任务，不应引入大范围重构。
- 如果检查发现前序实现偏离技术方案，应回到对应实现方案修正，不要在本方案里混入大型代码改动。
- 任务状态更新时必须保持 `任务追踪.md` 的 `步骤进度` 与本文件 `[ ]` / `[x]` 数量一致。
