# 目标

1. 完成对类：HttpRequestExtend 的修复。因为基础框架的变动，HttpRequestExtend，不再继承 HttpRequest。此外，仍然需要支持文件上传。

# 核心思路

1. 在 HttpRequestExtend 中新增类属性 List<FilePart>，这个代表接收到的上传文件。FilePart 的属性应该有 ioBuffer，这个代表上传文件的内容体，fileName，代表提取到的上传文件名，fieldName，代表提取到的属性名称。

2. 约束：业务方法入参不支持 `cc.jfire.jnet.extend.http.dto.HttpRequest`，只支持 `cc.jfire.boot.http.HttpRequestExtend`（以及 `Pipeline` 等已支持的内置注入类型）。`PathRequest` 中对 `HttpRequest.class` 的注入分支需要移除或直接抛出明确异常。
3. multipart/form-data 时：若 part 具备 `filename/filename*`（允许文件名为空字符串）或 `Content-Type: application/octet-stream`，则按文件/二进制处理并写入 `fileParts`（允许 `FilePart.fileName == null`）；其余 part 一律按 UTF-8 解析为文本字段写入 `paramMap`，避免在 `paramMap` 中存放 `IoBuffer` 引入生命周期/释放责任问题。

# 详细实现思路

## 基础框架变化分析

新版 `Jnet` 中的 `HttpRequest` 类发生了重大结构变化：

1. **属性迁移**：原来 `HttpRequest` 类中直接持有的 `method`, `url`, `version`, `headers`, `contentType`, `contentLength` 等属性，现在都移动到了内部的 `head` 属性（类型为 `HttpRequestPartHead`）中
2. **工具方法移除**：`HttpDecodeUtil` 中移除了以下方法：
   - `computePrefix(byte[])` - KMP 算法前缀计算
   - `findSubArray(IoBuffer, byte[], int[])` - KMP 算法查找子数组
   - `findContentType(Map, Consumer)` - 从 headers 中查找 Content-Type
3. **可用替代**：`IoBuffer` 提供了 `indexOf(byte[] array)` 方法可以替代 KMP 查找功能

## 重构方案

1. **HttpRequestExtend 不再继承 HttpRequest**：不持有 `HttpRequest` 引用，而是在 `from()` 方法中复制所需的关键属性，根据 `Content-Type` 完成必要的解析（如解析 fileParts），然后立即释放 `HttpRequest`
2. **新增 FilePart 类**：作为独立的顶层类，专门用于表示上传的文件
3. **删除 BinaryPart 类**：用 FilePart 替代其功能
4. **删除 BoundaryPart 内部类**：不再需要，multipart 解析直接生成 FilePart 和普通表单参数
5. **修复 PathRequest 中对 BinaryPart 的引用**：改为使用 FilePart

## Content-Type 解析策略

在 `HttpRequestExtend.from()` 方法中，根据 `Content-Type` 决定解析方式：

1. **multipart/form-data**：解析 boundary；若 part 含 `filename/filename*` 或 `Content-Type: application/octet-stream` 则写入 `fileParts`（允许 `fileName == null`）；其余 part 按 UTF-8 文本写入 `paramMap`；最后释放 body
2. **application/json**：将 body 解析为 `utf8StrBody` 字符串，然后释放 body
3. **其他类型**：将 body 解析为 `utf8StrBody` 字符串（如有），然后释放 body

# 实施步骤

## 步骤1：新建 FilePart 类

在 `cc.jfire.boot.http` 包下新建 `FilePart.java` 文件：

```java
package cc.jfire.boot.http;

import cc.jfire.jnet.common.buffer.buffer.IoBuffer;
import lombok.Data;
import lombok.experimental.Accessors;

@Data
@Accessors(chain = true)
public class FilePart implements AutoCloseable
{
    private String   fileName;
    private String   fieldName;
    private IoBuffer ioBuffer;

    @Override
    public void close()
    {
        if (ioBuffer != null)
        {
            ioBuffer.free();
            ioBuffer = null;
        }
    }
}
```

## 步骤2：删除 BinaryPart 类

删除文件 `src/main/java/cc/jfire/boot/http/BinaryPart.java`

## 步骤3：重构 HttpRequestExtend 类

将 `HttpRequestExtend.java` 完整重写为以下内容：

```java
package cc.jfire.boot.http;

import cc.jfire.dson.Dson;
import cc.jfire.jnet.common.api.Pipeline;
import cc.jfire.jnet.common.buffer.buffer.IoBuffer;
import cc.jfire.jnet.common.util.HttpDecodeUtil;
import cc.jfire.jnet.extend.http.dto.HttpRequest;
import lombok.Getter;
import lombok.Setter;

import java.net.URLDecoder;
import java.nio.charset.StandardCharsets;
import java.util.*;

@Getter
public class HttpRequestExtend implements AutoCloseable
{
    // 从 HttpRequest 复制的关键属性
    private             String              method;
    private             String              url;
    private             String              version;
    private             Map<String, String> headers;
    private             long                contentLength;
    private             String              contentType;

    // 解析后的属性
    private             String              utf8StrBody;
    private             Map<String, Object> paramMap;
    private             String              path;
    @Setter
    private             Pipeline            pipeline;
    protected           List<FilePart>      fileParts = DUMMY_FILE_PARTS;
    public static final List<FilePart>      DUMMY_FILE_PARTS = new LinkedList<>();

    public static HttpRequestExtend from(HttpRequest request, Pipeline pipeline)
    {
        if (request == null)
        {
            return null;
        }
        HttpRequestExtend extend = new HttpRequestExtend();
        extend.pipeline = pipeline;

        // 复制关键属性
        extend.method        = request.getHead().getMethod();
        extend.url           = request.getHead().getPath();
        extend.version       = request.getHead().getVersion();
        extend.headers       = request.getHead().getHeaders();
        extend.contentLength = request.getHead().getContentLength();
        extend.contentType   = extend.headers != null ? extend.headers.get("Content-Type") : null;

        // 解析 path 和 URL 参数
        extend.parsePath();

        // 根据 Content-Type 解析 body
        IoBuffer body = request.getBody();
        if (body != null && body.remainRead() > 0)
        {
            if (extend.contentType != null && extend.contentType.toLowerCase().startsWith("multipart/form-data"))
            {
                extend.parseMultiPart(body);
            }
            else
            {
                // application/json 或其他类型，解析为 utf8 字符串
                extend.utf8StrBody = StandardCharsets.UTF_8.decode(body.readableByteBuffer()).toString();
                body.free();
            }
        }

        // 释放 HttpRequest（head 部分）
        request.getHead().close();

        return extend;
    }

    @Override
    public void close()
    {
        fileParts.forEach(FilePart::close);
    }

    public Map<String, Object> getNotNullParamMap()
    {
        if (paramMap == null)
        {
            paramMap = new HashMap<>();
        }
        return paramMap;
    }

    private void parsePath()
    {
        int index = url.indexOf("?");
        if (index == -1)
        {
            path = url;
        }
        else
        {
            path = url.substring(0, index);
            paramMap = new HashMap<>();
            Arrays.stream(url.substring(index + 1).split("&")).forEach(v -> {
                int paramValueIndex = v.indexOf("=");
                if (paramValueIndex == -1)
                {
                    paramMap.put(v, "");
                }
                else
                {
                    paramMap.put(v.substring(0, paramValueIndex), v.substring(paramValueIndex + 1));
                }
            });
        }
    }

    public void parseJsonBodyToParamMap()
    {
        if (paramMap == null)
        {
            paramMap = new HashMap<>();
        }
        if (utf8StrBody != null)
        {
            Object o = Dson.fromString(utf8StrBody);
            if (o instanceof Map map)
            {
                paramMap.putAll(map);
            }
        }
    }

    private void parseMultiPart(IoBuffer body)
    {
        byte[] boundary = ("--" + contentType.substring(contentType.indexOf("boundary=") + 9)).getBytes(StandardCharsets.US_ASCII);
        int boundaryIndex = body.indexOf(boundary);
        if (boundaryIndex != body.getReadPosi())
        {
            throw new IllegalArgumentException("Invalid multipart data: boundary not found at expected position");
        }
        body.addReadPosi(boundary.length + 2);

        fileParts = new ArrayList<>();
        if (paramMap == null)
        {
            paramMap = new HashMap<>();
        }

        while (true)
        {
            boundaryIndex = body.indexOf(boundary);
            if (boundaryIndex != -1)
            {
                // 数据范围需要将回车换行去掉
                IoBuffer slice = body.slice(boundaryIndex - body.getReadPosi() - 2);
                parseBoundaryPart(slice);
                body.addReadPosi(2 + boundary.length + 2);
            }
            else
            {
                break;
            }
        }
        body.free();
    }

    private void parseBoundaryPart(IoBuffer slice)
    {
        // 解析 headers
        Map<String, String> partHeaders = new HashMap<>();
        HttpDecodeUtil.findAllHeaders(slice, partHeaders::put);

        // 解析 Content-Type
        String partContentType = partHeaders.get("Content-Type");
        if (partContentType == null)
        {
            partContentType = "text/plain";
        }

        // 解析 Content-Disposition
        String disposition = partHeaders.get("Content-Disposition");
        String fieldName = null;
        String fileName = null;

        int indexOfName = disposition.indexOf("name=");
        int endOfIndex = disposition.indexOf('"', indexOfName + 6);
        fieldName = disposition.substring(indexOfName + 6, endOfIndex);

        int index = disposition.indexOf("filename=");
        if (index != -1)
        {
            fileName = disposition.substring(index + 10, disposition.indexOf('"', index + 11));
        }
        if ((index = disposition.indexOf("filename*=UTF-8''")) != -1)
        {
            fileName = URLDecoder.decode(disposition.substring(index + 17), StandardCharsets.UTF_8);
        }

        // 根据规则决定处理方式：
        // - 有 filename/filename* 或 Content-Type 为 application/octet-stream：按文件/二进制处理
        // - 否则：按 UTF-8 文本字段处理
        boolean fileLike = fileName != null || "application/octet-stream".equalsIgnoreCase(partContentType);
        if (fileLike)
        {
            // 文件：添加到 fileParts
            FilePart filePart = new FilePart();
            filePart.setFileName(fileName);
            filePart.setFieldName(fieldName);
            filePart.setIoBuffer(slice);
            fileParts.add(filePart);
        }
        else
        {
            // 普通文本字段：解析为字符串存入 paramMap
            String value = StandardCharsets.UTF_8.decode(slice.readableByteBuffer()).toString();
            slice.free();
            paramMap.put(fieldName, value);
        }
    }
}
```

## 步骤4：修改 PathRequest 类

### 4.0 移除 HttpRequest 入参支持

当前 `PathRequest` 支持业务方法入参为 `HttpRequest`（见现有逻辑：`parameterTypes[i] == HttpRequestExtend.class || parameterTypes[i] == HttpRequest.class`）。本次改造明确：业务方法入参不支持 `HttpRequest`，只支持 `HttpRequestExtend`。

需要将上述判断改为只允许 `HttpRequestExtend.class`；若遇到 `HttpRequest.class`，直接抛出 `UnsupportedOperationException`（或等价的明确异常），提示开发者改用 `HttpRequestExtend`。

### 4.1 修改导入语句

将：
```java
import cc.jfire.boot.http.BinaryPart;
```

改为：
```java
import cc.jfire.boot.http.FilePart;
```

### 4.2 修改 BinaryPartParse 相关代码

将第99-101行：
```java
else if (List.class.equals(parameterTypes[i]) && method.getGenericParameterTypes()[i] instanceof ParameterizedType && ((ParameterizedType) method.getGenericParameterTypes()[i]).getActualTypeArguments()[0] == BinaryPart.class)
{
    paramValueGenerators[i] = new BinaryPartParse();
}
```

改为：
```java
else if (List.class.equals(parameterTypes[i]) && method.getGenericParameterTypes()[i] instanceof ParameterizedType && ((ParameterizedType) method.getGenericParameterTypes()[i]).getActualTypeArguments()[0] == FilePart.class)
{
    paramValueGenerators[i] = new FilePartParse();
}
```

### 4.3 修改 invoke 方法

由于 `HttpRequestExtend` 现在在 `from()` 方法中已经根据 `Content-Type` 完成了所有解析，`invoke` 方法需要简化。

将第127-149行的 `invoke` 方法：
```java
public Object invoke(HttpRequestExtend requestExtend) throws InvocationTargetException, IllegalAccessException
{
    switch (requestType)
    {
        case JsonPost ->
        {
            requestExtend.parseUtf8Value();
            if (needDeserializateJsonToParamMap && requestExtend.getUtf8StrBody() != null)
            {
                requestExtend.parseJsonBodyToParamMap();
            }
        }
        case UrlFormPost ->
        {
            throw new UnsupportedOperationException("还没有支持application/x-www-form-urlencoded形式");
        }
        case MultiPost ->
        {
            requestExtend.parseMultiPartToParamMap();
        }
    }
    return method.invoke(host, Arrays.stream(paramValueGenerators).map(gen -> gen.apply(requestExtend)).toArray());
}
```

改为：
```java
public Object invoke(HttpRequestExtend requestExtend) throws InvocationTargetException, IllegalAccessException
{
    switch (requestType)
    {
        case JsonPost ->
        {
            if (needDeserializateJsonToParamMap && requestExtend.getUtf8StrBody() != null)
            {
                requestExtend.parseJsonBodyToParamMap();
            }
        }
        case UrlFormPost ->
        {
            throw new UnsupportedOperationException("还没有支持application/x-www-form-urlencoded形式");
        }
        case MultiPost ->
        {
            // multipart 已在 HttpRequestExtend.from() 中解析完成，无需额外操作
        }
    }
    return method.invoke(host, Arrays.stream(paramValueGenerators).map(gen -> gen.apply(requestExtend)).toArray());
}
```

### 4.4 将 BinaryPartParse 类重命名并重写

将第230-249行的 `BinaryPartParse` 类：
```java
class BinaryPartParse implements Function<HttpRequestExtend, Object>
{
    @Override
    public Object apply(HttpRequestExtend httpRequestExtend)
    {
        return httpRequestExtend.getParamMap().values().stream()//
                                .filter(o -> {
                                    if (o instanceof HttpRequestExtend.BoundaryPart part)
                                    {
                                        return part.isBinary();
                                    }
                                    else
                                    {
                                        return false;
                                    }
                                })//
                                .map(p -> new BinaryPart().setData(((HttpRequestExtend.BoundaryPart) p).getData()).setFileName(((HttpRequestExtend.BoundaryPart) p).getFileName())//
                                                          .setFieldName(((HttpRequestExtend.BoundaryPart) p).getFieldName())//
                                ).toList();
    }
}
```

改为：
```java
class FilePartParse implements Function<HttpRequestExtend, Object>
{
    @Override
    public Object apply(HttpRequestExtend httpRequestExtend)
    {
        return httpRequestExtend.getFileParts();
    }
}
```

# 待办列表

1. 新建 FilePart 类
2. 删除 BinaryPart 类
3. 重构 HttpRequestExtend 类（不再继承 HttpRequest，复制关键属性后立即释放原请求）
4. 修改 PathRequest 类（将 BinaryPart 替换为 FilePart，简化 invoke 方法）

# 需要修改或新增的类

| 类型 | 类名 | 说明 |
|------|------|------|
| 新增 | cc.jfire.boot.http.FilePart | 新建文件上传表示类 |
| 删除 | cc.jfire.boot.http.BinaryPart | 删除原有的二进制部分类 |
| 修改 | cc.jfire.boot.http.HttpRequestExtend | 重构为复制属性方式，根据 Content-Type 解析后释放原请求，新增 fileParts 属性，移除 BoundaryPart 内部类 |
| 修改 | cc.jfire.boot.forward.path.PathRequest | 将 BinaryPart 替换为 FilePart，简化 invoke 方法 |
