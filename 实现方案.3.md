# 实现方案 3：实现 header 参数解析并替换 Content-Type boundary 截取

## 对应需求
- 需求编号：6, 7, 8, 20, 23, 25, 26, 36, 41, 43, 47, 53, 54, 55
- 需求描述：JfireBoot 需要结构化解析 `Content-Type` 参数，支持 quoted boundary 和 boundary 后续参数，并对缺失、空值或非法 boundary 返回受控 400。

## 对应技术步骤
- 技术步骤：步骤 3 - 实现通用 header 参数解析并替换 Content-Type boundary 截取
- 拆分原因：单一实现单元

## 技术方案依据
技术方案要求在 `HttpRequestExtend` 中新增通用 header value 参数解析能力，替换当前 `substring(contentType.indexOf("boundary=") + 9)` 的不安全逻辑。该解析器后续还会服务 `Content-Disposition`。

## 涉及文件
| 文件路径 | 修改类型 | 修改说明 |
|---------|---------|---------|
| `src/main/java/cc/jfire/boot/http/HttpRequestExtend.java` | 修改 | 新增 `HeaderValue`、参数解析方法、boundary 校验，并修改 `from()` 调用 |
| `src/test/java/MultipartExpectedBehaviorTest.java` | 修改/确认 | `quotedBoundaryShouldBeParsed`、`boundaryBeforeOtherParamsShouldBeParsed`、`missingBoundaryShouldReturnBadRequest` 应通过 |

## 代码改动指导
- **新增内容**：`HeaderValue` 内部类、`parseHeaderValue`、`splitHeaderSegments`、`unquote`、`extractBoundary`、`isValidBoundaryChar` 等私有方法。
- **修改内容**：修改 `from()` 中 multipart 判断逻辑，将 `parseMultiPart(body)` 改为传入解析后的 boundary。
- **删除内容**：删除 `parseMultiPart` 内对 `contentType.substring(...)` 的直接截取；实际删除在实现方案 4 完成。
- **代码完整性要求**：本方案必须给出完整新增方法块和 `from()` 中 body 分支完整改动单元。
- **完整代码或完整改动单元**：

~~~java
// 文件：src/main/java/cc/jfire/boot/http/HttpRequestExtend.java
// 新增 import
import java.nio.charset.CharsetEncoder;

// 在类字段区域新增常量。
private static final int MAX_BOUNDARY_LENGTH = 70;

// 替换 from() 中 “根据 Content-Type 解析 body” 的 body 分支。
IoBuffer body = request.getBody();
try
{
    if (body != null && body.remainRead() > 0)
    {
        HeaderValue parsedContentType = extend.contentType == null ? null : parseHeaderValue(extend.contentType);
        if (parsedContentType != null && "multipart/form-data".equalsIgnoreCase(parsedContentType.value()))
        {
            String boundary = extractBoundary(parsedContentType);
            extend.parseMultiPart(body, boundary);
        }
        else
        {
            extend.utf8StrBody = StandardCharsets.UTF_8.decode(body.readableByteBuffer()).toString();
            body.free();
        }
    }
}
catch (Throwable e)
{
    extend.close();
    throw e;
}
finally
{
    request.getHead().close();
}
return extend;

// 在 HttpRequestExtend 内新增私有 record 与辅助方法。
private record HeaderValue(String value, Map<String, String> params)
{
}

private static HeaderValue parseHeaderValue(String raw)
{
    if (raw == null)
    {
        return null;
    }
    List<String> segments = splitHeaderSegments(raw);
    if (segments.isEmpty())
    {
        throw HttpRequestParseException.badRequest("Invalid header value");
    }
    String value = segments.get(0).trim();
    if (value.isEmpty())
    {
        throw HttpRequestParseException.badRequest("Invalid header value");
    }
    Map<String, String> params = new HashMap<>();
    for (int i = 1; i < segments.size(); i++)
    {
        String segment = segments.get(i).trim();
        if (segment.isEmpty())
        {
            continue;
        }
        int eq = segment.indexOf('=');
        if (eq <= 0)
        {
            throw HttpRequestParseException.badRequest("Invalid header parameter: " + segment);
        }
        String name = segment.substring(0, eq).trim().toLowerCase(Locale.ROOT);
        String paramValue = segment.substring(eq + 1).trim();
        if (name.isEmpty())
        {
            throw HttpRequestParseException.badRequest("Invalid header parameter name");
        }
        params.put(name, unquote(paramValue));
    }
    return new HeaderValue(value, params);
}

private static List<String> splitHeaderSegments(String raw)
{
    List<String> result = new ArrayList<>();
    StringBuilder segment = new StringBuilder();
    boolean quoted = false;
    boolean escaped = false;
    for (int i = 0; i < raw.length(); i++)
    {
        char ch = raw.charAt(i);
        if (escaped)
        {
            segment.append(ch);
            escaped = false;
            continue;
        }
        if (quoted && ch == '\\')
        {
            segment.append(ch);
            escaped = true;
            continue;
        }
        if (ch == '"')
        {
            quoted = !quoted;
            segment.append(ch);
            continue;
        }
        if (ch == ';' && !quoted)
        {
            result.add(segment.toString());
            segment.setLength(0);
            continue;
        }
        segment.append(ch);
    }
    if (quoted)
    {
        throw HttpRequestParseException.badRequest("Invalid quoted header parameter");
    }
    result.add(segment.toString());
    return result;
}

private static String unquote(String value)
{
    if (value.length() >= 2 && value.charAt(0) == '"' && value.charAt(value.length() - 1) == '"')
    {
        StringBuilder builder = new StringBuilder(value.length() - 2);
        boolean escaped = false;
        for (int i = 1; i < value.length() - 1; i++)
        {
            char ch = value.charAt(i);
            if (escaped)
            {
                builder.append(ch);
                escaped = false;
            }
            else if (ch == '\\')
            {
                escaped = true;
            }
            else
            {
                builder.append(ch);
            }
        }
        if (escaped)
        {
            throw HttpRequestParseException.badRequest("Invalid quoted header parameter");
        }
        return builder.toString();
    }
    if (value.startsWith("\"") || value.endsWith("\""))
    {
        throw HttpRequestParseException.badRequest("Invalid quoted header parameter");
    }
    return value;
}

private static String extractBoundary(HeaderValue contentType)
{
    String boundary = contentType.params().get("boundary");
    if (boundary == null)
    {
        throw HttpRequestParseException.badRequest("Missing multipart boundary");
    }
    if (boundary.isEmpty())
    {
        throw HttpRequestParseException.badRequest("Empty multipart boundary");
    }
    if (boundary.length() > MAX_BOUNDARY_LENGTH)
    {
        throw HttpRequestParseException.badRequest("Multipart boundary is too long");
    }
    for (int i = 0; i < boundary.length(); i++)
    {
        if (!isValidBoundaryChar(boundary.charAt(i)))
        {
            throw HttpRequestParseException.badRequest("Invalid multipart boundary");
        }
    }
    return boundary;
}

private static boolean isValidBoundaryChar(char ch)
{
    return ch > 0x20 && ch < 0x7f && ch != '\r' && ch != '\n';
}
~~~

## 详细实现步骤

> 步骤状态使用 `[ ]` / `[x]` 跟踪。`tasks` 生成时全部为 `[ ]`；`implement` 每完成一个步骤后立即更新本清单。

### 实现步骤清单

- [x] 步骤 1：新增 header value 解析结构和方法
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：类内部，`parseMultiPart` 之前或之后均可
  - **修改内容**：新增 `HeaderValue`、`parseHeaderValue`、`splitHeaderSegments`、`unquote`。
  - **完成判定**：可以正确解析 `multipart/form-data; boundary="abc"` 与 `multipart/form-data; boundary=abc; charset=UTF-8`。
  - **说明**：该解析方法后续会被实现方案 5 复用。
- [x] 步骤 2：新增 boundary 提取与校验
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：header value 辅助方法附近
  - **修改内容**：新增 `MAX_BOUNDARY_LENGTH`、`extractBoundary`、`isValidBoundaryChar`。
  - **完成判定**：缺失、空值、超长和含控制字符的 boundary 抛出 `HttpRequestParseException.badRequest(...)`。
  - **说明**：boundary 不包含 body 中的前置 `--`。
- [x] 步骤 3：修改 `from()` 的 Content-Type 分支
  - **修改文件**：`src/main/java/cc/jfire/boot/http/HttpRequestExtend.java`
  - **修改位置**：`from(HttpRequest request, Pipeline pipeline)` 中 body 解析分支
  - **修改内容**：按完整改动单元替换 body 分支，使用 `parseHeaderValue` 判断 media type，并调用 `parseMultiPart(body, boundary)`。
  - **完成判定**：普通 JSON/urlencoded/其他 body 仍进入 utf8 字符串分支；multipart 使用结构化 boundary。
  - **说明**：`parseMultiPart(body, boundary)` 的完整实现由实现方案 4 承接，本方案可先调整方法签名。
- [x] 步骤 4：局部运行相关红灯测试观察收敛
  - **修改文件**：无
  - **修改位置**：无
  - **修改内容**：执行 `mvn -q -Dtest=MultipartExpectedBehaviorTest#quotedBoundaryShouldBeParsed,boundaryBeforeOtherParamsShouldBeParsed,missingBoundaryShouldReturnBadRequest test`。
  - **完成判定**：三个用例不再因 `Invalid multipart data: boundary not found at expected position` 失败；若仍因后续状态机失败，记录依赖实现方案 4。
  - **说明**：该方案可独立编译，但全部测试通过依赖后续方案。

## 依赖关系
- 前置实现方案：实现方案.2.md
- 后续实现方案：实现方案.4.md，实现方案.5.md，实现方案.6.md

## 测试要点
- quoted boundary 能被解包为 `abc`。
- boundary 后的 `charset=UTF-8` 不进入 boundary 值。
- 缺失 boundary 返回 400，而不是构造错误 boundary。

## 验证记录
> `tasks` 生成时先保留待填写状态；`implement` / `sub-implement` 在实际执行后回填。若单个实现方案不能独立完成全局编译，必须记录原因和依赖的后续方案，不能把未验证写成已通过。

- 局部验证：通过。已执行 `mvn -q -Dtest=MultipartExpectedBehaviorTest#quotedBoundaryShouldBeParsed,boundaryBeforeOtherParamsShouldBeParsed,missingBoundaryShouldReturnBadRequest test`。
- 全局编译：通过。实现方案 6 已完成完整 multipart 验收与 JfireBoot 默认测试；实现方案 7 已完成 Jnet 与 JfireBoot 的 `mvn -q compile`。
- 未全局编译通过原因：无
- 依赖后续方案：实现方案.4.md、实现方案.5.md
- 最终要求：全部实现方案完成后必须执行 `mvn compile`；若不是 Maven 项目，执行等价的只编译命令

## 注意事项
- `CharsetEncoder` import 如果最终未使用，不要保留。
- 参数解析遇到不成对引号必须 400。
- 不要把 `boundary` 参数名大小写敏感化。
