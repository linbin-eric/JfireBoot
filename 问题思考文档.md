# JfireBoot multipart/form-data 解析问题思考文档

## 背景

测试时间：2026-05-19

测试目标：复现线上日志中的异常，并继续寻找相邻的复现路径。

测试服务：当前 Workbench 本地实例，`http://127.0.0.1:8088`。

相关框架版本来自当前 classpath：

- `cc.jfire:JfireBoot:1.0.1-SNAPSHOT`
- `cc.jfire:Jnet:1.0.1-SNAPSHOT`

原始日志中的核心异常：

```text
java.lang.IllegalArgumentException: 尝试读取的内容过长，当前没有这么多数据
    at cc.jfire.jnet.common.buffer.buffer.impl.UnPooledBuffer.checkReadPosi(...)
    at cc.jfire.jnet.common.util.HttpCoderUtil.findAllHeaders(...)
    at cc.jfire.boot.http.HttpRequestExtend.parseBoundaryPart(...)
    at cc.jfire.boot.http.HttpRequestExtend.parseMultiPart(...)
```

这个异常发生在 `HttpRequestExtend.from()` 解析请求体阶段，早于路由匹配和业务 Controller 调用。

## 结论摘要

问题可以稳定复现，并且不依赖具体业务接口。只要请求头声明 `Content-Type: multipart/form-data`，框架就会先进入 multipart 解析，即使 URL 不存在，也会在路由匹配之前触发同类问题。

已发现的复现类型包括：

- part header 片段过短，触发 `UnPooledBuffer` 越界读取。
- `boundary` 参数解析不兼容合法变体，例如带引号或后续参数。
- part 缺少 `Content-Disposition` 或缺少 `name` 参数，触发 NPE 或字符串越界。
- part header 行没有冒号或只使用 LF 换行，客户端观察到无响应，疑似解析循环不推进。

合法 multipart 请求可以正常进入业务层，因此问题集中在畸形或边界格式请求的防御处理上。

## 最小复现

对任意路径发送以下请求即可复现原始日志中的同类异常：

```bash
curl -i -sS --max-time 5 -X POST http://127.0.0.1:8088/api/modules \
  -H 'Content-Type: multipart/form-data; boundary=abc' \
  --data-binary $'--abc\r\n\r\r\n--abc--\r\n'
```

返回结果：

```text
HTTP/1.1 200 OK

error:java.lang.IllegalArgumentException: 尝试读取的内容过长，当前没有这么多数据
```

对不存在的路径同样复现：

```bash
curl -i -sS --max-time 5 -X POST http://127.0.0.1:8088/__multipart_not_exist__ \
  -H 'Content-Type: multipart/form-data; boundary=abc' \
  --data-binary $'--abc\r\n\r\r\n--abc--\r\n'
```

这说明异常发生在框架请求解析层，而不是业务层。

## 测试基线

合法 multipart 请求：

```bash
curl -i -sS --max-time 5 -X POST http://127.0.0.1:8088/api/modules \
  -H 'Content-Type: multipart/form-data; boundary=abc' \
  --data-binary $'--abc\r\nContent-Disposition: form-data; name="field"\r\n\r\nvalue\r\n--abc--\r\n'
```

返回业务校验结果：

```text
{"success":false,"message":"仅支持 GET 请求"}
```

说明正常格式不会触发解析异常。

## 复现矩阵

| 用例 | 请求特征 | 客户端观察结果 |
| --- | --- | --- |
| `baseline_valid` | 标准 multipart 文本字段 | 正常进入业务层，返回“仅支持 GET 请求” |
| `initial_boundary_mismatch` | body 开头不是声明的 boundary | `IllegalArgumentException: Invalid multipart data: boundary not found at expected position` |
| `quoted_boundary` | `boundary="abc"`，body 使用 `--abc` | `Invalid multipart data` |
| `boundary_not_last_param` | `boundary=abc; charset=UTF-8` | `Invalid multipart data` |
| `missing_boundary_param` | `Content-Type: multipart/form-data` 无 boundary | `Invalid multipart data` |
| `empty_headers_lone_cr` | part header 只剩孤立 `\r` | `IllegalArgumentException: 尝试读取的内容过长，当前没有这么多数据` |
| `empty_header_block` | 空 header 块，无 `Content-Disposition` | `NullPointerException: disposition is null` |
| `header_without_colon` | part header 行没有 `:` | 2 秒内无响应 |
| `lf_only_part` | part 使用 LF 换行，不含 CRLF | 2 秒内无响应 |
| `no_content_disposition` | 有其他 header，无 `Content-Disposition` | `NullPointerException: disposition is null` |
| `disposition_missing_name` | `Content-Disposition: form-data` | `StringIndexOutOfBoundsException` |
| `disposition_unquoted_name` | `name=field` 未加引号 | `StringIndexOutOfBoundsException` |
| `filename_missing_quote` | `filename="x` 缺少结束引号 | `StringIndexOutOfBoundsException` |
| `final_boundary_only` | 只有结束 boundary，无 part | 被容忍，正常进入业务层 |
| `missing_crlf_after_boundary` | boundary 后缺少 CRLF | `NullPointerException: disposition is null` |

`header_without_colon` 和 `lf_only_part` 的“无响应”是客户端侧观察结果。结合 `HttpCoderUtil.findAllHeaders()` 的实现，原因很可能是解析 header 时找不到冒号或 CRLF，读指针没有推进，循环持续执行。

## 代码层推断

当前 `HttpRequestExtend.parseMultiPart()` 直接从 `Content-Type` 中查找 `boundary=` 并截取到字符串末尾：

```java
byte[] boundary = ("--" + contentType.substring(contentType.indexOf("boundary=") + 9)).getBytes(StandardCharsets.US_ASCII);
```

这里有几个问题：

- 没有处理缺失 `boundary` 的情况。
- 没有处理合法的 quoted-string，例如 `boundary="abc"`。
- 没有处理 boundary 后还有其他参数的情况，例如 `boundary=abc; charset=UTF-8`。
- 没有校验 boundary 值为空、非法字符或过长。

`HttpCoderUtil.findAllHeaders()` 在循环条件中直接读取 `readPosi + 1`：

```java
while (ioBuffer.get(ioBuffer.getReadPosi()) != '\r' || ioBuffer.get(ioBuffer.getReadPosi() + 1) != '\n')
```

当 slice 只有一个字节或没有完整 CRLF 时，这里会越界。

同时，如果 header 行没有冒号，也没有可识别的 CRLF，内部循环不会推进 `readPosi`，可能导致解析不返回。

`HttpRequestExtend.parseBoundaryPart()` 默认认为 `Content-Disposition` 一定存在，且一定包含引号形式的 `name="..."`：

```java
String disposition = partHeaders.get("Content-Disposition");
int indexOfName = disposition.indexOf("name=");
int endOfIndex = disposition.indexOf('"', indexOfName + 6);
fieldName = disposition.substring(indexOfName + 6, endOfIndex);
```

这会把协议错误转化成 NPE 或 `StringIndexOutOfBoundsException`，并且错误信息不利于定位客户端请求问题。

## 框架改进建议

### 1. 正确解析 Content-Type 参数

建议新增一个小的 Content-Type 参数解析方法，而不是直接 `substring`。

处理规则：

- media type 大小写不敏感。
- 参数名大小写不敏感。
- 参数值 trim。
- 支持 quoted-string：`boundary="abc"` 应解析为 `abc`。
- `boundary=abc; charset=UTF-8` 应解析为 `abc`。
- 缺失、空值或非法 boundary 应直接返回 400 级别错误。

### 2. 重写 multipart 边界推进逻辑

`parseMultiPart()` 当前对 boundary 后的两个字节默认为 CRLF，且普通 boundary 和结束 boundary 的推进规则混在一起。

建议显式区分：

- 起始 boundary：`--boundary\r\n`
- 普通 part 分隔：`\r\n--boundary\r\n`
- 结束 boundary：`\r\n--boundary--\r\n`

每一步都先校验剩余长度，再推进读指针。出现不符合格式的输入时，应抛出明确的 Bad Request 异常，而不是让底层 buffer 越界。

### 3. 让 header 解析器可失败、可停止

`HttpCoderUtil.findAllHeaders()` 建议改为返回解析结果，或在协议错误时抛出受控异常。

关键防护：

- 读取 `readPosi + 1` 前确认至少剩余 2 字节。
- 找不到 `:` 时，不要继续循环。
- 找不到行尾 CRLF 时，不要继续循环。
- 每轮循环后断言 `readPosi` 必须前进。
- 增加单个 header 行长度、header 总长度、header 数量限制。

这能同时解决越界和疑似死循环问题。

### 4. 严格校验 Content-Disposition

`parseBoundaryPart()` 应把以下情况转为 400：

- 缺少 `Content-Disposition`。
- `Content-Disposition` 不是 `form-data`。
- 缺少 `name` 参数。
- `name` 参数为空。
- `filename` 或 `filename*` 格式不合法。

建议复用通用的分号参数解析方法，不要通过固定下标和引号位置截取。

### 5. 统一框架层错误响应

当前框架捕获异常后返回：

```text
HTTP/1.1 200 OK
error:java.lang.IllegalArgumentException: ...
```

更合适的行为是：

- 请求协议或 body 格式错误：返回 `400 Bad Request`。
- 请求体过大：返回 `413 Payload Too Large`。
- 框架内部未预期错误：返回 `500 Internal Server Error`。

同时日志建议包含 method、url、Content-Type、Content-Length、User-Agent、远端地址。当前 `当前请求路径:` 为空，是因为异常发生在 `requestExtend.getPath()` 之前，排查信息不足。

### 6. 异常路径释放资源

`parseMultiPart()` 只在正常结束后 `body.free()`。如果 `parseBoundaryPart()` 抛异常，body 和部分 slice 可能没有按预期释放。

建议用 `try/finally` 管理 body，并明确 slice 所有权：

- 文本 part 解析完成后释放 slice。
- 文件 part 转交给 `FilePart` 后由 `FilePart.close()` 释放。
- 解析异常时释放当前 slice 和 body。

### 7. 增加框架级测试

建议在 JfireBoot/Jnet 层增加最小测试集：

- 标准文本字段。
- 标准文件字段。
- quoted boundary。
- boundary 后追加其他参数。
- 缺失 boundary。
- 起始 boundary 不匹配。
- 空 part header。
- 孤立 `\r`。
- LF-only body。
- header 无冒号。
- 缺失 `Content-Disposition`。
- `Content-Disposition` 缺失或错误的 `name`。
- `filename` 缺少结束引号。
- 结束 boundary only。

这些测试应断言：

- 合法请求能正常解析。
- 畸形请求不会卡住。
- 畸形请求不会抛出 NPE、字符串越界或底层 buffer 越界。
- 畸形请求返回明确的 400。
- 解析失败路径不会泄露 buffer。

## 当前判断

这不是 Workbench 业务代码的问题，而是 JfireBoot/Jnet multipart 解析器对畸形输入缺少边界检查和协议级错误建模。

短期可以在业务服务前放反向代理规则过滤异常 multipart 请求，但根因应该在框架层修复。优先级最高的是防止解析循环不返回，其次是把所有畸形 multipart 都收敛成明确的 400 响应。
