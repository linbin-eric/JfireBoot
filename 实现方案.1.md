# 实现方案 1：修复 Jnet 通用 HTTP header 解析器

## 对应需求
- 需求编号：1, 11, 12, 19, 36, 37, 38, 39, 48, 49
- 需求描述：修复 Jnet `HttpCoderUtil.findAllHeaders()` 的越界读取、缺少 CRLF/冒号时不受控、LF-only 不返回等问题，并保持合法 HTTP header 解析兼容。

## 对应技术步骤
- 技术步骤：步骤 1 - 修复 Jnet 通用 HTTP header 解析器
- 拆分原因：单一实现单元

## 技术方案依据
技术方案要求保持 `findAllHeaders(IoBuffer, BiConsumer)` 方法签名不变，通过按行推进、剩余长度检查、CRLF 检查、冒号检查和读指针推进检查来修复底层问题。合法 header 必须继续使用 `normalizeHeaderName()` 标准化，且不能修改 `UnPooledBuffer` 的越界保护语义。

## 涉及文件
| 文件路径 | 修改类型 | 修改说明 |
|---------|---------|---------|
| `/Users/linbin/Documents/代码/Jnet/src/main/java/cc/jfire/jnet/common/util/HttpCoderUtil.java` | 修改 | 替换 `findAllHeaders`，新增私有辅助方法 |
| `/Users/linbin/Documents/代码/Jnet/src/test/java/cc/jfire/jnet/common/util/HttpCoderUtilFixTest.java` | 修改/确认 | 修复后必须全部通过 |
| `/Users/linbin/Documents/代码/Jnet/src/test/java/cc/jfire/jnet/common/util/HttpCoderUtilProblemReproducer.java` | 修改/确认 | 可保留为问题复现说明；若与新行为冲突，后续改为历史说明型测试 |

## 代码改动指导
- **新增内容**：在 `HttpCoderUtil` 中新增 `findCrlf`、`findByte`、`decodeRange`、`invalidHeader` 私有静态方法。
- **修改内容**：完整替换 `findAllHeaders` 方法。
- **删除内容**：删除原 `findAllHeaders` 中两个不保证推进的扫描循环。
- **代码完整性要求**：修改已有文件必须提供完整方法和完整辅助方法块；不得只写“增加校验”。
- **完整代码或完整改动单元**：

~~~java
// 文件：/Users/linbin/Documents/代码/Jnet/src/main/java/cc/jfire/jnet/common/util/HttpCoderUtil.java
// 替换原 findAllHeaders 方法，并在其后追加这些私有辅助方法。
public static void findAllHeaders(IoBuffer ioBuffer, BiConsumer<String, String> consumer)
{
    while (true)
    {
        int lineStart = ioBuffer.getReadPosi();
        int writePosi = ioBuffer.getWritePosi();
        if (writePosi - lineStart < 2)
        {
            throw invalidHeader("incomplete header block");
        }
        if (ioBuffer.get(lineStart) == '\r')
        {
            if (ioBuffer.get(lineStart + 1) == '\n')
            {
                ioBuffer.setReadPosi(lineStart + 2);
                return;
            }
            throw invalidHeader("CR must be followed by LF");
        }

        int lineEnd = findCrlf(ioBuffer, lineStart, writePosi);
        if (lineEnd == -1)
        {
            throw invalidHeader("header line must end with CRLF");
        }
        int colon = findByte(ioBuffer, lineStart, lineEnd, (byte) ':');
        if (colon == -1)
        {
            throw invalidHeader("header line missing ':'");
        }

        String headerName = decodeRange(ioBuffer, lineStart, colon).trim();
        if (headerName.isEmpty())
        {
            throw invalidHeader("header name is empty");
        }

        int valueStart = colon + 1;
        while (valueStart < lineEnd)
        {
            byte b = ioBuffer.get(valueStart);
            if (b != ' ' && b != '\t')
            {
                break;
            }
            valueStart++;
        }
        int valueEnd = lineEnd;
        while (valueEnd > valueStart)
        {
            byte b = ioBuffer.get(valueEnd - 1);
            if (b != ' ' && b != '\t')
            {
                break;
            }
            valueEnd--;
        }

        String headerValue = decodeRange(ioBuffer, valueStart, valueEnd);
        ioBuffer.setReadPosi(lineEnd + 2);
        if (ioBuffer.getReadPosi() <= lineStart)
        {
            throw invalidHeader("read position did not advance");
        }
        consumer.accept(normalizeHeaderName(headerName), headerValue);
    }
}

private static int findCrlf(IoBuffer ioBuffer, int start, int end)
{
    for (int i = start; i + 1 < end; i++)
    {
        if (ioBuffer.get(i) == '\r')
        {
            if (ioBuffer.get(i + 1) == '\n')
            {
                return i;
            }
            throw invalidHeader("CR must be followed by LF");
        }
    }
    return -1;
}

private static int findByte(IoBuffer ioBuffer, int start, int end, byte target)
{
    for (int i = start; i < end; i++)
    {
        if (ioBuffer.get(i) == target)
        {
            return i;
        }
    }
    return -1;
}

private static String decodeRange(IoBuffer ioBuffer, int start, int end)
{
    int oldReadPosi = ioBuffer.getReadPosi();
    ioBuffer.setReadPosi(start);
    String value = StandardCharsets.UTF_8.decode(ioBuffer.readableByteBuffer(end)).toString();
    ioBuffer.setReadPosi(oldReadPosi);
    return value;
}

private static IllegalArgumentException invalidHeader(String reason)
{
    return new IllegalArgumentException("Invalid HTTP header: " + reason);
}
~~~

## 详细实现步骤

> 步骤状态使用 `[ ]` / `[x]` 跟踪。`tasks` 生成时全部为 `[ ]`；`implement` 每完成一个步骤后立即更新本清单。

### 实现步骤清单

- [x] 步骤 1：替换 Jnet header 解析循环
  - **修改文件**：`/Users/linbin/Documents/代码/Jnet/src/main/java/cc/jfire/jnet/common/util/HttpCoderUtil.java`
  - **修改位置**：`HttpCoderUtil.findAllHeaders`
  - **修改内容**：用“完整代码或完整改动单元”中的 `findAllHeaders` 完整替换现有方法。
  - **完成判定**：方法内不再直接读取 `readPosi + 1` 而不检查剩余长度，不再存在找不到冒号/CRLF 后继续 `consumer.accept(headerName, headerValue)` 的路径。
  - **说明**：这是修复越界读取和 LF-only 不返回的核心。
- [x] 步骤 2：新增私有辅助方法
  - **修改文件**：`/Users/linbin/Documents/代码/Jnet/src/main/java/cc/jfire/jnet/common/util/HttpCoderUtil.java`
  - **修改位置**：`findAllHeaders` 方法之后、`findContentLength` 方法之前
  - **修改内容**：新增 `findCrlf`、`findByte`、`decodeRange`、`invalidHeader` 四个私有静态方法。
  - **完成判定**：`findAllHeaders` 只通过辅助方法扫描 CRLF 和冒号；异常信息包含 `Invalid HTTP header`。
  - **说明**：`HttpCoderUtilFixTest` 依赖该异常消息。
- [x] 步骤 3：确认合法 header 兼容性
  - **修改文件**：`/Users/linbin/Documents/代码/Jnet/src/test/java/cc/jfire/jnet/common/util/HttpCoderUtilFixTest.java`
  - **修改位置**：`validHeadersCanBeParsed`、`emptyHeaderBlockCanBeParsed`
  - **修改内容**：保留现有断言；如修复后出现 value trim 行为差异，只允许调整测试输入，不允许改变合法解析结果。
  - **完成判定**：合法 header 测试通过，`Host`、`Content-Type` 仍被标准化。
  - **说明**：避免底层修复造成 HTTP codec 回归。
- [x] 步骤 4：运行 Jnet 单测验证
  - **修改文件**：无
  - **修改位置**：无
  - **修改内容**：在 `/Users/linbin/Documents/代码/Jnet` 执行 `mvn -q test-compile` 和 `mvn -q -Dtest=cc.jfire.jnet.common.util.HttpCoderUtilFixTest test`。
  - **完成判定**：`HttpCoderUtilFixTest` 6 个用例全部通过。
  - **说明**：Jnet 全量测试如果包含长耗时网络用例，可在验证记录中说明未执行范围。

## 依赖关系
- 前置实现方案：无
- 后续实现方案：实现方案.6.md，部分影响实现方案.5.md

## 测试要点
- `loneCrShouldFailWithControlledHeaderError` 不再触发底层 buffer 越界消息。
- `lfOnlyHeaderShouldFailWithoutHanging` 必须在 500ms 内返回并抛出 `IllegalArgumentException`。
- `headerWithoutColonShouldFailWithControlledHeaderError` 不能再产生 null header name。

## 验证记录
> `tasks` 生成时先保留待填写状态；`implement` / `sub-implement` 在实际执行后回填。若单个实现方案不能独立完成全局编译，必须记录原因和依赖的后续方案，不能把未验证写成已通过。

- 局部验证：通过。已在 `/Users/linbin/Documents/代码/Jnet` 执行 `mvn -q test-compile` 和 `mvn -q -Dtest=cc.jfire.jnet.common.util.HttpCoderUtilFixTest test`。
- 全局编译：通过。实现方案 6 已完成 Jnet 安装和双仓库测试闭环；实现方案 7 已完成 Jnet 与 JfireBoot 的 `mvn -q compile`。
- 未全局编译通过原因：无
- 依赖后续方案：实现方案 6 执行双仓库完整验证流程
- 最终要求：全部实现方案完成后必须执行 `mvn compile`；若不是 Maven 项目，执行等价的只编译命令

## 注意事项
- 不要修改 `UnPooledBuffer` 的越界保护来掩盖问题。
- `decodeRange` 会临时调整 `readPosi`，必须恢复旧值。
- 当前仓库路径是 Jnet，不是 JfireBoot；实施时注意工作目录。
